# Test plan

Mirrors `docs/feature-matrix.md` row for row, by the same ID. This repository has three automated
lanes: `cd pipeline && uv run pytest` (in `pipeline/tests/test_pipeline.py`), Astro's build-time
checks (`npx astro check`, `npm run build`), and — since #34 — `npm run test:unit`, which is
`node --test` over `src/**/*.test.ts` — no test framework, no runner dependency, relying on Node's
native type stripping (**Node >= 22.18 / >= 23.6 required**). The one package it did cost is
`@types/node`, needed so that `npx astro check` can resolve `node:test` and `node:assert/strict`
and keep typechecking the test files rather than skipping them. There is still no e2e or
component-rendering
runner: `test:unit` covers pure TypeScript modules only (today, `src/components/charts/scales.ts`
and `src/components/charts/format.ts`),
so React island and interaction coverage remains manual and named explicitly rather than left
implicit. `test:unit` is not wired into CI: `deploy.yml` only builds (on `node-version: 22`, which
clears the type-stripping floor), and `refresh-data.yml` runs pytest on its monthly schedule only.
Wiring the lanes into a per-push check is #67; until then, the pytest source-level guard
`test_nice_extent_zero_anchors_a_non_negative_series` is what holds the `niceExtent` rule
unattended.

## Introduction route

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| INTRO-1 | the generic conformance suite in `tests/test_accessibility.py`, which picks up `dist/index.html` from its `dist/**/index.html` glob | `npx astro check`, `npm run build`, `grep -o 'href="/income-tax/economy"' dist/index.html` | none — no chart, no interaction | the page is #43's placeholder, not the front door; #48 replaces it wholesale and rewrites this row |

## Economy route

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| ECO-1 | `test_real_gdp_is_positive_in_every_fiscal_year`, `test_nominal_gdp_fy1995_matches_the_budget_route_denominator` | `npx astro check`, `npm run build`, `grep -o '<svg' dist/economy/index.html` (no-JS render proof) | 390px width and greyscale checks (`.claude/plans/issue-12.md` V-16, V-17) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio; 390px with scripting on **FAIL** — #64, #66, #73; 390px with scripting off **FAIL** — the `<noscript>` type mitigation never applies, #78 | no automated JS coverage of `RealGdpLogScale.tsx`'s focus/readout interaction; proved only by the pytest data invariants plus the manual checks |
| ECO-2 | `test_output_per_hour_and_median_income_share_1984_to_2024` | `npx astro check`, `npm run build` | same 390px/greyscale checks as ECO-1 — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio (worst co-occurring pair 1.95:1, carried by in-plot end labels and table columns); 390px FAILs #64, #66, #73, #78 | no automated JS coverage of `GrowthAndShadow.tsx`'s interaction |
| ECO-3 | `test_unemployment_peak_over_actuals_is_fy1983`, `test_participation_peak_over_actuals_is_fy2000`, `test_fy2020_unemployment_is_a_fiscal_year_average_not_a_monthly_peak` | `npx astro check`, `npm run build` | same 390px/greyscale checks as ECO-1 — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio (worst co-occurring pair 1.95:1, carried by in-plot end labels and table columns); 390px FAILs #64, #66, #73, #78 | no automated JS coverage of `WhoWorks.tsx`'s two-panel interaction |
| ECO-4 | `test_rate_series_start_at_their_documented_first_year`, `test_no_rate_series_is_negative_and_the_minima_are_near_zero`, `test_cpi_inflation_is_negative_in_fy1955_and_fy2009`, `test_nice_extent_zero_anchors_a_non_negative_series` | `npx astro check`, `npm run build`, `grep -o '<svg' dist/economy/index.html` (no-JS render proof) | tab/live-region parity (M1), table contents, 390px width (M4/M5) and greyscale (M6) checks in `.claude/plans/issue-13.md` — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: M1 keyboard **executed**, FAILs #69, #71; M4/M5 **FAIL** — #64, #66, #73, #78; M6 greyscale **PASS** with a computed per-panel worst-pair luminance ratio — this figure holds the route's tightest pair, 1.03:1 between the 3-month bill and the 10-year note, carried by in-plot end labels and three table columns; live-region *announcement* parity is a screen-reader property, the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80** | no automated JS coverage of `PricesAndRates.tsx`'s two-panel interaction; proved only by the pytest data invariants plus the manual checks |
| ECO-5 | `test_wage_and_profit_share_are_gdp_shares_and_do_not_sum_to_100`, `test_fy2020_share_moves_are_denominator_artefacts` | `npx astro check`, `npm run build` | same M1, M4/M5 and M6 checks as the other Economy-route figures — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio (5.54:1); 390px FAILs #64, #66, #73, #78 | no automated JS coverage of `LaborAndCapital.tsx`'s interaction |
| ECO-6 | — (prose-only section, no chart or new field) | `npx astro check`, `npm run build`, `grep -o 'id="limits"'`/`grep -o 'href="/income-tax/households/"'` on `dist/economy/index.html` | none needed — no chart, no interaction | none |

## Government route

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| GOV-1 | `test_debt_chart_uses_the_shared_unit_toggle`, `test_section_1_spells_out_its_trillions`, `test_no_dollar_axis_labels_a_zero_tick_in_billions` (all `test_chart_vocabulary.py`); `test_every_figure_server_renders_its_chart_svg`, `test_government_section_1_renders_its_whole_apparatus_without_scripting` (`test_accessibility.py`, #36 — SSR presence of the chart `<svg>` and of §1's whole no-JS apparatus, run against the `npm run build` output) | `npx astro check`, `npm run build`, `npm run test:unit` (`format.test.ts` — `tick`'s zero case and `trillionsLong`'s byte-exactness against the `fmtFull` #35 deleted); `grep -o '<svg' dist/government/index.html | wc -l` → 14, all 14 `class="chart"` | keyboard walk of the two-option toggle, and the live readout at 390px | point focus/hover geometry — which datum a given pointer position or Tab press selects — is unproven; the source-level and built-HTML guards fix the *text* each state announces, not the hit-testing that chooses the state |
| GOV-2 | `test_crossing_date_is_reconciled_not_competing`, `test_foreign_share_always_carries_its_denominator`, `test_public_split_is_keyed_to_its_denominator`, `test_section_2_uses_no_party_colours`, `test_federal_reserve_absent_from_rendered_section_2`, `test_tic_revision_note_is_carried`, `test_curated_snapshots_expose_their_as_of` | `npx astro check`, `npm run build` (`curatedVintage` throws at build time if `debtHolders._meta.refresh.mode` is not `curated`) | M1 keyboard **executed 2026-08-24** (Chrome 151; FAILs #69, #71), M4/M5 390px **executed** (JS on #64/#66/#73/#74, JS off #78), M6 greyscale **executed 2026-08-26** — PASS with a computed per-panel luminance ratio, M2 screen reader **NOT EXECUTED, human + assistive technology required (#80)**; results in `docs/contracts/accessibility.md` § Manual pass results | no automated JS coverage of `DebtHolders.tsx`'s bar/connector geometry; "View as table" (M6) could not be verified against `dist/government/index.html` because Radix `Collapsible.Content` does not render its children in the closed-state SSR output (also true of the pre-existing `DebtChart.tsx` table, not a regression here) — table correctness (units, `no data` cells) is proved by the component's row-building logic and by `TableView`'s own `?? 'no data'` contract, not by a static grep of the built HTML. **Superseded by A11Y-1's D1 fix**: `TableView` now renders via a native `<details>`/`<summary>` element, present in server-rendered HTML regardless of scripting, so the Radix-SSR gap noted above no longer applies — re-verify M6 with a static grep against `dist/government/index.html` |
| GOV-3 | `test_section_2_uses_no_party_colours` (covers both islands), `test_maturity_instruments_are_not_an_exhaustive_partition`, `test_maturity_percentages_are_never_derived_from_amounts`, `test_maturity_history_is_not_charted`, `test_curated_snapshots_expose_their_as_of` | `npx astro check`, `npm run build` | M1 keyboard **executed 2026-08-24** (Chrome 151; FAILs #69, #71), M4/M5 390px **executed** (JS on #64/#66/#73/#74, JS off #78), M6 greyscale **executed 2026-08-26** — PASS with a computed per-panel luminance ratio, M2 screen reader **NOT EXECUTED, human + assistive technology required (#80)**; results in `docs/contracts/accessibility.md` § Manual pass results | no automated JS coverage of `DebtMaturity.tsx`'s band/marker geometry; "View as table" (M6) has the same Radix-SSR gap noted under GOV-2, now also superseded by A11Y-1's D1 fix |
| GOV-4 | `test_outlay_components_sum_to_total_in_every_unit_family`, `test_party_control_is_null_outside_fy1995_2025`, `test_net_mandatory_is_positive_in_every_year`, `test_every_unit_family_covers_the_full_span`, `test_surplus_years_are_positive_deficit_values` | `npx astro check`, `npm run build` | M1–M12 in `.claude/plans/issue-2.md` (unit-sum spot check, control strip absent pre-1995, keyboard inspection parity, GDP-view clipping, no-JS render, 390px width, greyscale) | no automated JS coverage of `BudgetChart.tsx` — the stacking, strip and inspector logic are proved only by the pytest data invariants above plus the manual checks; the `npm run test:unit` runner added in #34 covers pure modules only and does not render this island, so the gap is open. Since #33 this module no longer implements the laws-to-splits join — it calls the shared `src/components/laws/join.ts`, guarded at the source level by `test_the_laws_to_splits_join_has_exactly_one_implementation`; the unmatched-law throw is still unexercised at runtime because all 23 laws join |
| GOV-5 | `test_section5_revenue_and_outlay_means_hold`, `test_section5_surplus_band_is_exactly_fy1998_2001` | `npx astro check`, `npm run build` | M1, M2, M4, M5, M6, M7 in `.claude/plans/issue-5.md` (surplus band reads in greyscale, FY2020 does not clip in any unit view, keyboard/live-region parity, table view, no-JS render, 390px width) | no automated JS coverage of `StructuralGap.tsx` — the fill segmentation, hatch pattern and per-datum focus logic are proved only by the pytest data invariants above plus the manual checks |
| GOV-6 | `test_section6_mandatory_growth_is_quoted_on_the_net_basis`, `test_nice_extent_zero_anchors_a_non_negative_series` | `npx astro check`, `npm run build` | M2, M4, M5, M6, M7 in `.claude/plans/issue-5.md` (FY2020 does not clip in any unit view, keyboard/live-region parity, table view, no-JS render, 390px width) | no automated JS coverage of `VotedAndNot.tsx` — the net-basis computation and FY2015 marker are proved only by the pytest data invariant above plus the manual checks |
| GOV-7 | `test_section7_net_interest_share_of_deficits_is_39_percent`, `test_section7_series_low_is_not_the_fy2015_trough`, `test_net_interest_series_low_is_fy2003` | `npx astro check`, `npm run build` | M3, M4, M5, M6, M7, M8 in `.claude/plans/issue-5.md` (FY2003 bar plainly non-zero, keyboard/live-region parity, table view, no-JS render, 390px width, cross-link scrolls to `#how-old`) | no automated JS coverage of `NetInterest.tsx` — the zero-anchored bar geometry and trough/series-low vocabulary are proved only by the pytest data invariants above plus the manual checks |
| GOV-8 | `test_every_law_joins_to_a_counted_split`, `test_filter_totals_render_to_the_published_two_places`, `test_the_two_1997_laws_carry_no_score_but_do_carry_votes`, `test_cares_house_cell_has_no_countable_vote`, `test_vp_tiebreak_laws_are_exactly_the_three_named`, `test_narrowest_chamber_margin_is_defined_for_every_law`, `test_deficit_share_exists_for_every_enactment_fiscal_year`, `test_section_8_no_longer_claims_classified_composition`, `test_no_document_still_calls_vote_composition_classified`, `test_law_split_totals_round_the_sum_not_the_displays`, `test_check_laws_rejects_a_drifted_split_total`, `test_the_laws_to_splits_join_has_exactly_one_implementation` | `npx astro check`, `npm run build`, `uv run python build.py --tier monthly`, `uv run python build.py --tier monthly --dry-run` | M1–M14 in `.claude/plans/issue-3.md` (filter/totals spot checks, basis toggle, CARES/1997/50-50 rendering, margin and score sort stability, keyboard-only Select/sort/selection, no-JS render, 390px width, greyscale) | `npm run test:unit` (added in #34) renders no React island, so `LawExplorer.tsx`'s filter, sort, selection and basis-toggle logic is proved only by the pytest data invariants above (`derive.ts`'s pure functions are mirrored in Python for the pipeline tests, not unit-tested directly in TS) plus the manual checks. Since #33 the laws-to-splits join is single-sourced in `src/components/laws/join.ts`, shared with GOV-9; `test_the_laws_to_splits_join_has_exactly_one_implementation` is a source-level guard on that (it cannot exercise the join at runtime — `test:unit` covers pure modules, not islands) |
| GOV-9 | `test_attribution_both_breakdowns_reconcile_to_the_same_total`, `test_attribution_coalition_totals_match_the_published_finding`, `test_attribution_president_totals_match_the_published_finding`, `test_attribution_gross_and_net_differ_by_the_reductions`, `test_attribution_excludes_the_two_1997_laws`, `test_attribution_party_line_side_is_counted_not_classified`, `test_attribution_every_law_joins_to_a_counted_split`, `test_the_laws_to_splits_join_has_exactly_one_implementation` | `npx astro check`, `npm run build` (runs `aggregate.ts`'s import-time reconciliation invariant) | M1–M8 in `.claude/plans/issue-4.md` (keyboard tab switch + bar traversal, screen-reader announcement, no-JS render, 390px width, greyscale, table disclosure totals, reduced-motion, contrast) | no automated JS coverage of `AttributionSplit.tsx` — the bar geometry, tab wiring and live-region text are proved only by the pytest reconciliation tests above plus the manual checks; the `npm run test:unit` runner added in #34 covers pure modules only and does not render this island, so the gap is open. Since #33 this module no longer implements the laws-to-splits join — it calls the shared `src/components/laws/join.ts`, guarded at the source level by `test_the_laws_to_splits_join_has_exactly_one_implementation`; the unmatched-law throw is still unexercised at runtime because all 23 laws join |
| GOV-10 | `test_revenue_components_sum_in_the_gdp_and_share_families`, `test_miscellaneous_revenue_is_never_zero`, `test_revenue_series_is_contiguous_and_has_no_null_fields`, `test_customs_is_the_fastest_growing_revenue_line`, `test_oecd_average_is_flagged_and_the_country_list_is_a_selection`, `test_oecd_and_federal_revenue_are_marked_as_different_scopes` (plus the pre-existing `test_revenue_components_sum_to_total`), `test_nice_extent_zero_anchors_a_non_negative_series`, `uv run python build.py --tier monthly --dry-run` (0 validation failures) | `npx astro check`, `npm run build` | M1–M7 in `.claude/plans/issue-7.md` (no-JS render, 390px width, keyboard sweep parity, greyscale, view-toggle consistency, table units, screen-reader announce order) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: no-JS render **executed**; 390px **FAIL** — #62, #64, #66, #73, #78; keyboard parity **executed**, FAILs #69, #71, #72; greyscale **PASS (note)** — this chart holds the site's worst co-occurring pair, `--rev-pr` against `--rev-eg` at **1.00:1**, non-adjacent in the stack, tightest adjacent band 1.44:1, four of seven sources labelled in-plot and all seven carried as table columns; announce order is **NOT EXECUTED — human plus an assistive technology required, #80** | the axis-domain rule these charts share is covered by `npm run test:unit` (`src/components/charts/scales.test.ts`, added in #34 — `RevenueChart`'s nominal view is one of the two axes it zero-anchors); there is still no automated JS coverage of `RevenueChart.tsx`/`OecdChart.tsx` interactions — the stacking, focus/readout and dot-plot logic are proved only by the pytest data invariants above plus the (unrun) manual checks, since `test:unit` renders no island. Since #33 this module no longer implements the laws-to-splits join — it calls the shared `src/components/laws/join.ts`, guarded at the source level by `test_the_laws_to_splits_join_has_exactly_one_implementation`; the unmatched-law throw is still unexercised at runtime because all 23 laws join |
| GOV-11 | 10 tests (`pytest -k state`): 51 in-grid jurisdictions, DC flagged and colour-excluded, territory give/get asymmetry, no derived field silently zero, give/get same fiscal year, jurisdiction give sums reconcile to the national total, ratio null-or-positive, tax-mix shares in `[0,100]` or null, `not_levied` vs missing distinct, no party-colour token in the section source | `npx astro check`, `npm run build`, plus `check_schema`/`check_states` in `lib/validate.py` (JSON Schema + 8 reconciliation invariants, gates every `--tier monthly` run) | keyboard/live-region parity across all 51 tiles into the sortable table (Enter/Space to sort), colour never carries meaning alone, 390px legibility, JS-disabled render, focus-ring visibility — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: keyboard walk **executed**, FAILs #69, #71; live-region *announcement* parity (one live region per chart, not per data point) is a screen-reader property and is **NOT EXECUTED — human plus an assistive technology required, #80** — the 2026-08-24/26 pass covered the Tab-focusable half only and this row must not be read as claiming otherwise; 390px **FAIL** — #63, #74, #78; JS-disabled render **executed**, all 13 tables present with scripting off; focus ring **executed** in Chrome 151 and WebKit 26.5 — a ring paints, but its 1.5px thickness fails WCAG 2.2 Focus Appearance (#75), and Safari.app itself is **NOT EXECUTED — human plus an assistive technology required, #80**. Greyscale **PASS (note)**, which is where *colour never carries meaning alone* was checked and where it holds: the cartogram's adjacent scale steps separate by 1.00:1, with direction carried by a `+`/`−` glyph on every tile and magnitude by the table; the state tax-mix bar separates by 1.44:1 with no legend and no in-plot label, its three segments named only inside the disclosure table; every colour-coded category is therefore also reachable as text — the state postal code on each tile, the three category names as columns of the tax-mix disclosure table — so no category is distinguished by hue alone; structural proxies for all four are in `.claude/plans/issue-14.md` Definition of done 8/11/12 | no browser-driven test exists anywhere in this repo (consistent with GOV-1/GOV-4); the tile grid's 51-target keyboard walk and the sortable table's Enter/Space activation are unverified by anything but the manual pass |
| GOV-12 | `test_crisis_years_are_a_third_of_all_borrowing`, `test_deficit_debt_gap_matches_the_curated_resolution`, `test_limits_section_quotes_the_curated_deficit_debt_gap`, `test_limits_section_does_not_call_the_votes_classified` (unchanged by #39 — limit 6 is worded without the `classif` stem so the existing whole-section guard passes as written; the test was **not** narrowed to make room for the copy), `test_exactly_one_chamber_vote_is_absent` | `npx astro check`, `npm run build` | M1–M5 in `.claude/plans/issue-8.md` (no-JS render, 390px width, tab order, greyscale, screen reader) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: no-JS render **executed**, every `<details>` table present with scripting off; 390px with scripting off **FAIL** — the `<noscript>` type mitigation never applies, #78; tab order **executed**, FAILs #69, #71, #72; greyscale **PASS** with a computed per-panel worst-pair luminance ratio; the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80** | no JS runner, but §12 ships no JS so this is a non-gap. **Nothing asserts the number of `<li>` in `<ol class="limits">`** — the heading says "Six" and the list carries six as of #39, but the two could drift apart without any test noticing; the count is checked by hand against `dist/government/index.html` at review time |

## Households route

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| HH-1 | `test_median_income_reproduces_the_published_figures`, `test_1947_has_a_gini_but_no_median_income`, `test_series_start_years_match_the_declared_coverage`, `test_absent_observations_are_null_not_zero` | `npx astro check`, `npm run build`, static-render greps against `dist/households/index.html` (verification 5 in `.claude/plans/issue-9.md`) | M1, M2, M6–M9 in `.claude/plans/issue-9.md` (slider keyboard operation, datum focus parity, table units, 390px width, greyscale) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results. Scope correction: **no slider renders** — the site has zero `<input>` and zero `<select>` elements, so the slider keyboard item has no consumer. Datum focus parity **executed**, FAILs #69, #71; 390px **FAIL** — #64, #66, #73, #78; greyscale **PASS** with a computed per-panel worst-pair luminance ratio (single series, nothing carried by colour); the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80** | no automated JS coverage of `MedianIncome.tsx` or `YearRange.tsx` interactions; of the manual items above only the screen-reader half is still unexecuted (#80), the rest were run in a browser |
| HH-2 | `test_family_gini_reproduces_the_published_figures`, `test_cbo_top1_share_is_two_published_points_not_a_series`, `test_gini_is_labelled_as_families`, `test_absent_observations_are_null_not_zero` | `npx astro check`, `npm run build`, static-render greps against `dist/households/index.html` | M1, M2, M3–M6, M9, M10 in `.claude/plans/issue-9.md` (empty-range state, no-connector check, table units, greyscale, VoiceOver) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio (single series per panel); 390px **FAIL** — #64, #66, #73, #78; the VoiceOver item is the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80** | no automated JS coverage of `HouseholdSpread.tsx`; of the manual items above only the VoiceOver half is still unexecuted (#80) |
| HH-3 | `test_bracket_history_reproduces_curated_top_rates`, `test_bracket_history_adjustment_years_are_documented`, `test_bracket_thresholds_carry_both_nominal_and_constant_dollars`, `test_filing_statuses_are_not_projected_backwards`, `test_bracket_count_runs_from_two_in_1988_to_fiftysix_in_1918`, `test_bracket_history_absent_values_are_null_not_zero`; `check_bracket_history` at the build validation gate | `npx astro check`, `npm run build`; static-render greps in `.claude/plans/issue-10.md` V6 (both sections present, `scaleLog` used, SVGs server-rendered) | M1 (Tab-focus/live-region parity), M2 ("View as table" + units), M3 (`no data` not `0` pre-1949/1952), M4 (no-JS render), M5 (390px legibility), M6 (greyscale), M7 (log-axis label), M8 (VoiceOver) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: M1 **executed**, FAILs #69, #71; M4 **executed**, table present with scripting off; M5 **FAIL** — #64, #66, #73, #78; M6 **PASS (note)** — worst co-occurring pair 1.31:1, but the three panels are separate plots each with its own in-plot title; M8 is **NOT EXECUTED — human plus an assistive technology required, #80** | no automated JS coverage of `BracketHistory.tsx` interactions; of M1-M8 only M8 (VoiceOver) is still unexecuted (#80) |
| HH-4 | `test_cbo_effective_rates_are_anchor_points_not_a_series`, `test_cbo_effective_rates_basis_names_payroll_tax`; `check_cbo_effective_rates` at the build validation gate | `npx astro check`, `npm run build`; static-render greps (`Nobody pays the top rate on their whole income`, `includes payroll tax`) | M4 (no-JS render), M6 (greyscale, marker shapes distinguish the five groups without colour), M8 (VoiceOver) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: M4 **executed**, table present with scripting off, but 390px with scripting off **FAIL** — the `<noscript>` type mitigation never applies, #78; M6 **PASS, confirmed in greyscale** — the five income groups are drawn as circle, square, triangle, diamond, × and +, with a shape legend, so the 1.06:1 colour separation carries nothing on its own; M8 is the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80** | no automated JS coverage of `StatutoryVsEffective.tsx` interactions; the no-connector (anchor-point, not line) rendering is proved by manual inspection of the 2026-08-26 greyscale render, which shows the six groups as unconnected shape markers |
| HH-5 | `test_top1_income_share_is_present_and_paired`, `test_percentile_groups_are_nested_not_a_partition`, `test_unpublished_group_cells_are_absent_not_zero` | `npx astro check`, `npm run build` | M1 (390px legibility), M2 (keyboard parity), M3 (greyscale), M4 (screen-reader pass over `WhoPays`, absent cells announce as "no data") in `.claude/plans/issue-11.md` | no automated JS coverage of `WhoPays.tsx` / `Top1TaxShare.tsx` interactions — the nesting, absence and non-annual invariants are proved only by the pytest data checks above plus manual review |
| HH-6 | none new — reuses `check_revenue`'s FY2025 `g_pr`/`s_pr` assertions in `pipeline/lib/validate.py`, run via `uv run python build.py --tier monthly --dry-run` | `npx astro check`, `npm run build` | M1 (390px, end labels not overlapping), M2 (keyboard parity), M3 (greyscale) in `.claude/plans/issue-11.md` | no automated JS coverage of `PayrollBill.tsx`'s two-view toggle |
| HH-7 | none — prose only, no chart | `npx astro check`, `npm run build` | manual review of the five numbered limits against the grep-pinned phrases in `.claude/plans/issue-11.md` | no chart, so no data-invariant test applies |

## Reference pages

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| REF-1 | `test_sources_doc_describes_counted_splits`, plus the whole of DATA-3 — since #39 the register is gated rather than merely rendered, so `test_check_sources_is_clean_against_the_real_tree` is also REF-1 coverage: it fails if a source this site cites is missing from the document this route publishes | the `if (!entry) throw` guard in `src/pages/sources.astro`, plus a `grep` on `dist/sources/index.html` | M2 (390px width, table scroll containment) in `.claude/plans/issue-8.md` — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: **FAIL** — `/sources/` measures `documentElement.scrollWidth` 520px against a 390px `clientWidth`, from three unbroken `<code>` spans; filed as #79. No table renders on this route, so scroll containment does not apply | none |

## Cross-cutting

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| DATA-1 | `test_every_published_output_has_a_schema` (the population is `src/data/*.json`, not the schemas that exist — no exemption set today, and if one is ever needed it is an explicit named frozenset, never a weakened assertion), `test_every_schema_names_a_published_output` (no orphan schema left behind by a rename), `test_check_schema_fails_when_an_output_has_no_schema` (**the guard-bites proof** — `validate.SCHEMA_DIR` is pointed at a copy of `pipeline/schemas/` with `budget.schema.json` removed and the call must yield exactly one failure naming `budget` and the path `schemas/budget.schema.json`; the same test asserts the inverse, zero failures against the intact directory, so it cannot pass by failing for an unrelated reason), plus the two sibling cases `test_check_schema_reports_malformed_json_rather_than_raising` and `test_check_schema_reports_an_invalid_schema_document_rather_than_raising`, and `test_every_schema_rejects_a_realistic_corruption` — 14 parametrized cases, one per output including the two pre-existing schemas, ids reading `budget-offsetting-receipts-turned-positive` and the like, sourced from `sorted(DATA.glob("*.json"))` rather than a hand-typed list so output number fifteen collects as `NO-CORRUPTION-CASE` and fails | `check_schema` in `pipeline/lib/validate.py`, run over every `*.json` in the staging directory by `build.py`; verified by hand on 2026-08-26 by moving `pipeline/schemas/economy.schema.json` aside — `uv run python build.py --tier monthly --dry-run` exited 1 with `BUILD FAILED`, `economy: no schema at schemas/economy.schema.json` and `Nothing was written.` | none — the gate is fully automated, and the guard-bites check is the automated analogue of #36's manual `client:only` flip | Schemas pin **shape and range only**: object keys, types, array `minItems`, numeric bounds and enums. Cross-field reconciliation (components summing to totals, the FY2003 series low, the non-partition rule, denominator checks) stays in `validate.py`'s per-output `check_*` functions and is not duplicated here. Bounds are set loose on purpose (`minItems` below today's length, `oecd.us_rank` unbounded above) so a legitimate source revision passes and only a real regression fails — see E5 in `.claude/plans/issue-37.md`. Nothing validates the schemas on the TypeScript side; `src/data/types.ts` is still hand-maintained and could drift from the schema without any test noticing |
| DATA-2 | `test_chained_and_core_cpi_start_at_their_own_first_year` (every row before FY2002 / FY1958 is `is None`, never `0` — 0 is falsy and 0 is the bug, so falsiness is not accepted; every row from the start year to the last projected row is non-null), `test_chained_and_core_cpi_are_index_levels_not_rates` (pins FY2002 `chained_cpiu == 105.132` and FY1958 `core_cpiu == 29.458` — a year-over-year rate would be single digits, so these values fix the series as index LEVELS and fail loudly if a future CBO vintage rebases either index), `test_phantom_zero_row_guard_rejects_a_duplicate_in_any_other_year` (the **ingest** guard-bites proof — `_drop_phantom_zero_row` is called directly with three cases in one test so it cannot pass by failing for an unrelated reason: the same duplicate shape at `(1986, "single")` raises `SourceUnavailable` with a message naming the duplicated floor and the phrase `refusing to guess`; the known `(1985, "single")` case drops the **phantom** and leaves the real `hi == 2390` row; a clean ladder is returned untouched), `test_check_bracket_history_rejects_a_duplicate_bracket_floor` (the **published-output** guard-bites proof — `validate.DATA_DIR` is pointed at a `tmp_path` copy of `bracket_history.json`: a single duplicated floor in 1985 single yields exactly one failure naming the year, the status and the floor; the same corruption in 1994 mfj proves the check is generic over every ladder, not 1985-special; the full phantom shape trips the 1985 fingerprint as well; and the inverse against the real data yields zero failures) | `check_bracket_history` in `pipeline/lib/validate.py` — strictly increasing `lo` in every year/status ladder plus the 1985-single fingerprint (16 brackets, exactly one 0% bracket ending at $2,390, no open-ended zero-rate bracket, top 50% above $85,130), run on every build including `--tier monthly` because `build.py` copies `src/data` into staging. `check_schema` against `pipeline/schemas/economy.schema.json`, which now requires `chained_cpiu` and `core_cpiu` as `["number","null"]` with `not: {const: 0}`, so a zero-fill fails the build. `check_prose_figures` reconciles 118 registered figures including the new `debt_holders.data.split.1.share_pct`. Gate: `uv run python build.py --tier monthly --dry-run` | none — every check here is automated | **Nothing on the site consumes `chained_cpiu` or `core_cpiu` yet.** #38's scope is "emit"; §4 still charts `cpi` and `core_pce` only, so the two new series have pipeline invariants as their sole coverage and no component or e2e test touches them. The duplicate-floor check reads the published output, so it cannot catch a corruption introduced between `emit.write` and a hand-edit of `src/data`; and the 1985 fingerprint is a spot check on one ladder, not a full re-derivation of all 113 years against the primary source |
| DATA-3 | `test_source_register_covers_every_published_output` (the population is `src/data/*.json`, not the register entries that happen to exist, and the converse is asserted in the same test so an entry orphaned by a rename cannot keep the count whole), `test_check_sources_is_clean_against_the_real_tree` (**the inverse, asserted first** — zero failures against the real `SOURCES.md` and the real `src/data/`, so nothing below can pass by failing for an unrelated reason), `test_check_sources_fails_when_a_cited_source_is_not_registered` (**the guard-bites proof for rule B**, the defect #39 was filed for — `validate.SOURCES_DOC` is pointed at a `tmp_path` copy of `SOURCES.md` with a source's lead-in replaced, and the call must yield exactly one failure naming that register key and `SOURCES.md`; run for `irs_soi_table_5` and `census_stc` in one test body so the guard is proven generic over the register rather than special-cased to whichever source happened to be missing), `test_check_sources_fails_when_an_output_cites_an_unregistered_source` (**the guard-bites proof for rule D**, the case B cannot see — `validate.DATA_DIR` is pointed at a `tmp_path` copy of `src/data/` with `"; Some Unregistered Source"` appended to `states_tax_mix.json`'s `_meta.source`; exactly one failure, naming the output and the unaccounted text), `test_check_sources_tolerates_a_vintage_refresh` (**the loose-on-purpose proof** — `budget.json`'s `Feb 2026` rewritten to `February 2027` yields zero failures, so a future author cannot tighten `_normalize_source` into an exact match and turn every ordinary upstream republication red, which is how the check would end up disabled), `test_normalize_source_strips_dates_but_keeps_identifying_numbers` (**the counterweight to that proof** — vintage tolerance must not be bought by erasing every digit, so this asserts each date form normalizes away *and* that `Table 5`/`Table 23`, `MEHOINUSA672N` and `PL 115-97` survive and stay distinct), `test_check_sources_fails_when_a_cited_document_number_changes` (**the guard-bites proof for the third direction** — `cbo_effective_rates.json`'s `Supplemental Data Table 9` rewritten to `Table 4` leaves the wording and the registered source untouched yet must still yield exactly one failure, which only a normalizer that keeps identifying digits can see) | `check_sources` in `pipeline/lib/validate.py`, called from `run()` **unconditionally** alongside `check_meta` and `check_schema` — never behind an `if "x" in outputs:` gate, because a check skipped because its output was not in the tier reads exactly like a check that passed (#37). Gate: `uv run python build.py --tier monthly --dry-run`; verified by hand on 2026-08-26 by replacing the `**IRS Statistics of Income, SOI Data Book Table 5...**` lead-in in `SOURCES.md` — the run exited 1 with `irs_soi_table_5: cited by this site but NOT registered in SOURCES.md` and `Nothing was written.` | none — the gate is fully automated, and both guard-bites directions have automated proofs | The register is matched **into** `SOURCES.md`, never scraped **out of** it, so nothing asserts that a registration is a *good* description of its source — only that the string is present. `_normalize_source` strips **dates only** — years, month names, day-of-month and a Revenue Procedure's `-NN` serial — so two sources differing only in an identifying number (`Table 5` vs `Table 23`) no longer collapse onto each other; the residual risk is inverted, in that a genuine vintage carried in some digit form the normalizer does not know about would turn the build red on an ordinary refresh. Rule D compares a stored shape, so it catches a source **added** to a source line but not a source silently **swapped** for one already in the register under the same citation string. And nothing checks the reverse direction of the register itself: a `SOURCES.md` entry cited by no output (CBO's Estimates of Automatic Stabilizers today) is simply absent from `sources.yaml` and is not reported |
| DATA-4 | Four offline cache-contract tests in `pipeline/tests/test_pipeline.py`, all of which `monkeypatch.setattr(lib.fetch, "CACHE_DIR", tmp_path)` — which works because `_cache_path` reads the module global at call time — prime an entry by hand in the exact on-disk shape, and aim the call at an unroutable `.invalid` host, so a cache hit returns and a cache miss can only raise: that is what makes them prove *no request happened* rather than merely that the right value came back. `test_warm_text_cache_is_served_without_a_request` (the `{url, text, retrieved_at}` shape, `from_cache is True`), `test_binary_cache_round_trips_through_base64` (the `{url, b64, retrieved_at}` shape, bytes back byte-for-byte, including non-UTF-8 payloads), `test_post_json_cache_key_includes_the_payload` (**both directions in one body** — payload A is served from a hand-primed entry, proving the `url + "|" + json.dumps(payload, sort_keys=True)` derivation is intact, and payload B must raise `SourceUnavailable` rather than quietly return A's body, which is the USASpending fiscal-year collision), and `test_a_binary_cache_entry_does_not_crash_a_text_fetch` (**the one intentional behaviour delta of #40, pinned** — it fails with `KeyError: 'text'` on the pre-#40 module and passes after, so the guard cannot be silently dropped). Plus the pre-existing `test_unreachable_source_raises_rather_than_returning_empty` and `test_truncated_body_is_treated_as_failure`, which exercise the same unified core through `fetch` | n/a — the pipeline is not part of the Astro build | The warm-cache probe, run on #40: `STAMP=$(date +%s); sleep 1; cd pipeline && uv run python build.py --tier monthly --dry-run >/dev/null && find .cache -newermt "@$STAMP" | wc -l` must print `0`. Zero cache files rewritten is zero HTTP requests, which is the only way to observe that a refactor did not silently invalidate the 67 warm entries and turn a warm run into a full refetch that still reports success | The two older failure tests hit the real network and would **also pass during an outage**, since an outage raises the same `SourceUnavailable` they assert — a green run of those two is weaker evidence than it looks. No test covers the non-200 or short-body branches offline (both need a stub transport, which would be the first test double in this suite). `post_json`'s decode-before-cache ordering is asserted only by reading the code, not by a test. `pipeline/lib/sources.py:145` builds its own `httpx.Client` for a `HEAD` probe and is outside this core entirely — parked, not covered |
| DATA-5 | `test_check_meta_titles_is_clean_against_the_real_tree` (**the inverse, asserted first** — zero failures over all 14 published outputs, so nothing below can pass by failing for an unrelated reason), `test_no_curated_title_types_a_literal_year` and `test_curated_titles_render_to_their_published_form` (rule A at the source, plus the proof that templating reworded only the two outputs #41 aimed at and left the other twelve byte-identical), `test_check_meta_titles_bites_on_the_defect_it_was_filed_for` (**the guard-bites proof** — the exact pre-#41 curated budget title is restored through `curated.meta_for` and the call must yield exactly one failure naming `budget` and `curated/notes.yaml`; this is the case a coverage-only comparison cannot see, because FY1995-FY2025 *is* a declared window, `control_start`/`control_end`, just not the one the sentence attached it to), `test_check_meta_titles_fails_a_range_coverage_does_not_declare` (rule B, `validate.DATA_DIR` pointed at a `tmp_path` copy with budget retitled FY1900-FY2025), `test_check_meta_titles_fails_a_range_with_no_coverage_at_all` (`coverage: null` is **unknown, not pass** — a range added to `income_tax_by_group`'s title yields one failure naming `TITLE_RANGE_EXEMPT`), `test_the_only_exemption_carries_a_written_reason_and_is_not_a_silent_skip` (both halves: the entry names why, and with the dict emptied `cbo_effective_rates` goes red under both rules — so the exemption is what holds it, not a regex that cannot see the range), `test_open_ended_titles_assert_a_start_and_not_an_end` (`economy.coverage.end` is the 2036 projection horizon; "FY1950 onward" asserts the start only, and FY1949 fails), `test_iso_date_coverage_bounds_compare_as_years` (`party_splits` carries ISO dates, not integers), `test_nested_per_series_coverage_blocks_are_windows_a_title_may_name`, and `test_expand_title_raises_rather_than_publishing_a_placeholder` (a `{start}` in a published JSON file is worse than the stale range it replaced, so emit raises on both no-coverage and unknown-key, and passes a placeholder-free title through verbatim) | `check_meta_titles` in `pipeline/lib/validate.py`, called from `run()` **unconditionally** beside `check_meta`, `check_schema` and `check_sources` — never behind an `if "x" in outputs:` gate (#37); gate: `uv run python build.py --tier monthly --dry-run`, verified on 2026-08-26 by restoring the literal budget title, which exited non-zero before the fix and clean after | none — the guard is fully automated, and the guard-bites direction has an automated proof | Rule A reads `curated/notes.yaml`, so it cannot see a title typed straight into `src/data/*.json` by hand; rule B is what covers that case, and rule B is deliberately loose — it accepts **any** declared window, so a templated clause pointing at the wrong one of two windows would still pass. The regex knows two title shapes, a `YYYY-YYYY` range and `YYYY onward`; a title inventing a third ("since 1962", "through the 1990s") is not checked at all and is not detected as unchecked. Nothing asserts a title is a *good* description of its data, only that its years agree with coverage |
| DATA-6 | `test_brief_file_list_paths_exist` (every first token in the `## Files in this folder` fence resolves under the repository root; the parser **raises rather than returning empty** if the section or its fence moves, since a check that finds nothing to check reads exactly like one that passed — #37's rule applied to a doc), `test_brief_file_list_names_the_brief_itself_and_every_root_json` (the two claims existence alone cannot catch: the first line named `README.md` as "this brief", and `README.md` **does** exist, so a resolving path was still the wrong file; and the eight curated inputs are asserted present with no `data/` or `content/` prefix, globbed from the root rather than hand-listed so a ninth input cannot be quietly omitted) | none — `BRIEF.md` is not part of the Astro build | Guard-bites verified by hand on 2026-08-26: `oecd-tax.json` moved aside, `uv run pytest -k brief` went red naming the missing path, green again on restore | **Existence-only, deliberately.** Nothing asserts the list is *exhaustive*: a new root file is not required to appear, because an exhaustiveness check would fight every addition to the repository and end up disabled — the failure mode that let this block drift for the length of the build. Nothing checks the prose description beside each path either, so a file whose contents change purpose keeps its old one-line gloss. `test_brief_file_list_names_the_brief_itself_and_every_root_json` closes the exhaustiveness gap for the root JSON inputs only, which is the half that actually drifted |
| A11Y-1 | `pipeline/tests/test_accessibility.py` — a generic static conformance suite over every built page and every island component | `npx astro check`, `npm run build` | The itemised "Shared" checklist in `docs/contracts/accessibility.md` (Tab/Shift-Tab traversal, screen-reader pass, focus-trap check, 390px legibility, greyscale render, Safari focus-ring visibility, measured pixel contrast) — **executed 2026-08-24 / 2026-08-26**, Chrome 151 and WebKit 26.5, results one row per check per route in `docs/contracts/accessibility.md` § Manual pass results, now eight checks (M1–M8) at 32 rows (8×4 routes). Seven of the eight ran; the screen-reader pass (M2) did not, and the Safari focus-ring item (M7) ran only in headless WebKit, not Safari.app — both carried by #80 | No browser-driven test of any kind exists in this repo, so the checks above are a dated manual observation rather than a regression guard — they were run by hand and nothing re-runs them. #67 adds the CI check that would. #80 holds the two items no agent can execute |
| A11Y-2 | none | none | The itemised "Per-consumer" checklist in `docs/contracts/accessibility.md` (cross-route keyboard sweep, screen-reader passes per route, keyboard-model checks per interactive primitive) — cross-route keyboard sweep **executed 2026-08-24**, Chrome 151, tab stops counted per route (`/` 408, `/government/` 471, `/households/` 356), FAILs #69, #71, #72; keyboard-model checks **executed** against the three control shapes that actually render — `role="radio"` groups, filter dropdown buttons and `<details>`/`<summary>` disclosures — the anticipated `Select`/`Slider`/`Dialog`/`Tabs`/`Tooltip` consumers never landed and the site renders zero `<input>` and zero `<select>`; screen-reader passes **NOT EXECUTED — human plus an assistive technology required, #80** | Same automation gap as A11Y-1. The row sits at `In progress` rather than `Shipped` because #80 is open; it is the named blocker, not an open-ended one |
| A11Y-3 | `pipeline/tests/test_accessibility.py` — seven new checks: `test_nav_bar_mirrors_every_route_and_section` (per page, set equality of the panel's route hrefs and contents fragments against the rail's, so the two lists cannot drift — 5 routes everywhere, 12/7/6/0/0 sections by route), `test_nav_bar_does_not_precede_the_skip_link` (document-order index of `.skip-link` below that of the bar's `<summary>`), `test_no_page_repeats_an_id` (generic; the duplicated contents list made a second `id="toc-heading"` the live hazard, and `id_map` silently keeps the last of a duplicate so every existing `aria-labelledby` check would still have passed), `test_nav_bar_panel_scrolls_internally`, `test_sticky_nav_bar_offsets_its_anchor_targets`, `test_nav_bar_open_close_is_not_animated` and `test_nav_bar_tap_targets_clear_44px` — the last four read a new `narrow_media_block()` helper, which returns the raw body of `@media (max-width: 62rem)` and **raises rather than returning `""`** if that block moves, because `iter_css_rules` flattens every @-rule and discards its condition and a helper that finds nothing to check reads exactly like one whose checks passed. Plus `".navbar a"` appended to `_TEXT_SELECTORS`, which holds the bar's link colour to the same 4.5:1 floor as `.rail a` via `test_no_text_selector_paints_with_a_low_contrast_token` (it fails hard if the selector is absent from `global.css`, so it cannot pass by finding nothing). Guard-bites verified by hand on 2026-08-26: stripping `overflow-y: auto`, dropping `#main`'s `scroll-margin-top`, adding a `transition` to `.navbar-chevron`, lowering the tap-target floor to 2rem, renaming `navbar-toc-heading` back to `toc-heading` and slicing one route out of the panel each turned exactly the intended test red | `npx astro check` (0 errors), `npm run build`, `npm run test:unit` (17/17), plus the built-output greps in `.claude/plans/issue-42.md` § Verification — one `class="navbar"` per page, 24/14/12 `data-section` on Government/Households/Economy, zero `navbar-toc-heading` on `/` and `/sources`, exactly one `id="toc-heading"` on Government, and the **desktop-unchanged proof**: once the `.navbar` block, the inline `<script>` and the `data-section` attributes are removed and the stylesheet content-hash normalised, `dist/government/index.html` and `dist/index.html` are byte-identical to their pre-change builds | **EXECUTED 2026-08-26**, Chromium via Playwright, `/`, `/economy`, `/households`, `/government`, `/sources`. At 390×844: the bar is `position: fixed` with `top === 0` and `height === 52` at `scrollY = 0` and at the document bottom on all five routes; Government's 12-entry panel has `scrollHeight 804 > clientHeight 776` while `document.body.scrollHeight` is identical (25,329) closed, open and closed again — and the same in **landscape 844×390** (804 > 322, body 22,920 throughout), which is the worst case; with all 13 tables open the page grows 25,329→37,018px and the bar is still pinned at `top 0` at `scrollY 36,174` and still opens. Tab stop 1 `.skip-link`, stop 2 the `<summary>` (accessible name "Government — routes and contents"), stop 3 inside `main`; Enter opens and focus lands on `#navbar-panel`; Escape closes and focus returns to the `<summary>`; Tab from the last panel link lands inside `main`, not wrapped; an in-panel contents link closes the panel and `#net-interest` lands at `top 64 >= 52`; an outside click closes it; the `/government/` filter dropdowns still own Escape and still restore their own focus. Every `a` and `summary` inside `.navbar` measures ≥ 44×44 on all five routes (6/12/13/18/6 elements). With `javaScriptEnabled: false`: 17 links in the closed DOM, opens by click and by Enter, closes by Enter, and Escape correctly does nothing. At 1440×900: `.navbar` computes `display: none`, `.rail` computes `position: sticky`, and a full Tab traversal of `/government/` counted 438 stops with **zero** inside `.navbar` | No browser-driven regression guard exists for any of the manual column — the same gap as A11Y-1 and A11Y-2; #67 is the issue that would add it, and nothing re-runs the Playwright sequence above. Whether the native disclosure **announces** its expanded/collapsed state correctly is a screen-reader question and is **NOT EXECUTED** — #80. Section-level `aria-current` in the panel is #44 and scroll restoration is #46, both deliberately out |
| A11Y-4 | `pipeline/tests/test_accessibility.py` — five new checks: `test_no_built_page_ships_a_section_level_aria_current` (per page; zero `aria-current="true"` nodes in the server-rendered HTML **and** `aria-current="page"` at exactly 2, so a build that dropped the route markers entirely could not turn the zero-check green), `test_every_contents_anchor_is_addressable_by_the_spy` (per page; every `.toc` and `.navbar-toc` anchor carries a `data-section` and the two lists' values equal `main section[id]` in document order — a section added to a page but not to its `sections` array is otherwise silently unmarkable; `/` and `/sources`, which pass no `sections` prop, return early, matching the IIFE's own early return), `test_contents_lists_are_not_live_regions` (per page; no `.toc`/`.navbar-toc`, no ancestor and no descendant carries `aria-live`, `aria-atomic` or a live-region `role` — **this is the proof of the silence criterion**, not an assumption), `test_section_state_selector_is_scoped_and_not_bare` (over `global.css` via `iter_css_rules`: at least one `[aria-current='true']` rule scoped to the contents list, and **no** bare `[aria-current]` selector anywhere — it raises rather than passing silently if it finds no scoped rule at all) and `test_the_section_spy_introduces_no_scripted_scrolling` (over `BaseLayout.astro`: exactly one `<script is:inline>` block, `IntersectionObserver` present in it so the check cannot pass by finding no script, and none of `scrollIntoView`, `scrollTo(`, `scrollBy(`, `behavior:`). Guard-bites verified by hand on 2026-08-26: hard-coding `aria-current` onto the rail's first anchor, dropping `data-section` from the panel list, adding `aria-live="polite"` to `.toc`, widening the CSS selector to a bare `[aria-current]` and adding a `scrollIntoView` call to the IIFE each turned exactly the intended test red | `npx astro check` (0 errors), `npm run build`, `npm run test:unit` (17/17, unchanged — no TS module is touched), `uv run pytest` (298 passed, 4 skipped), plus the built-output greps in `.claude/plans/issue-44.md` § Verification: `aria-current="true"` **0** and `aria-current="page"` **2** on all five built pages, `IntersectionObserver` present in `dist/government/index.html` (`is:inline` scripts are not bundled, so it lands in the page), zero `\.rail [a-z]*\[aria-current\]` in the built CSS, and zero `scrollIntoView|scrollTo\(|behavior:` in `dist/government/index.html` | **PENDING** — the Playwright sequence in `.claude/plans/issue-44.md` § Rendered | No browser-driven regression guard exists for the manual column — the same gap as A11Y-1, A11Y-2 and A11Y-3. The 200px-step monotonicity sweep is the **only** anti-oscillation evidence and nothing re-runs it; #67 is the issue that would. Whether a screen reader **reports** the marked section on demand is not observable without an assistive technology and is **NOT EXECUTED — #80** (checklist item 13); only the silence half is provable statically. Scroll restoration across a route change is #46 and is deliberately out |

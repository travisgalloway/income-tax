# Test plan

Mirrors `docs/feature-matrix.md` row for row, by the same ID. This repository has three automated
lanes: `cd pipeline && uv run pytest` (in `pipeline/tests/test_pipeline.py`), Astro's build-time
checks (`npx astro check`, `npm run build`), and — since #34 — `npm run test:unit`, which is
`node --test` over `src/**/*.test.ts` — no test framework, no runner dependency, relying on Node's
native type stripping (**Node >= 22.18 / >= 23.6 required**). The one package it did cost is
`@types/node`, needed so that `npx astro check` can resolve `node:test` and `node:assert/strict`
and keep typechecking the test files rather than skipping them. There is still no e2e or
component-rendering
runner: `test:unit` covers pure TypeScript modules only (today, `src/components/charts/scales.ts`
and `src/components/charts/format.ts`),
so React island and interaction coverage remains manual and named explicitly rather than left
implicit. `test:unit` is not wired into CI: `deploy.yml` only builds (on `node-version: 22`, which
clears the type-stripping floor), and `refresh-data.yml` runs pytest on its monthly schedule only.
Wiring the lanes into a per-push check is #67; until then, the pytest source-level guard
`test_nice_extent_zero_anchors_a_non_negative_series` is what holds the `niceExtent` rule
unattended.

## Introduction route

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| INTRO-1 | the generic conformance suite in `tests/test_accessibility.py`, which picks up `dist/index.html` from its `dist/**/index.html` glob — **23 cases**. Since #48 the page passes a `sections` prop, so three of them stop early-returning and assert for real: `test_every_contents_anchor_is_addressable_by_the_spy` (the four `data-section` anchors in both lists equal `main section[id]` in document order), `test_route_nav_and_contents_nav_are_separate_landmarks`, and `test_nav_bar_mirrors_every_route_and_section`. `test_no_built_page_ships_a_section_level_aria_current` pins `aria-current="page"` at 2 and `="true"` at 0. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #52, the same file carries Criterion 1's mechanical half over every `<section id>` on this route: `test_every_section_with_a_figure_states_its_question_first`, `test_every_section_with_a_figure_answers_after_it` (split on the **last** `</figure>`, so a two-figure section is not passed by a paragraph sitting between its charts), `test_no_standfirst_preempts_its_finding` (number-token Jaccard strictly below `PREEMPTION_CEILING` = 0.5), `test_no_section_heading_names_the_charts_construction`, and `test_the_criterion_one_audit_covers_every_section` (the contract's audit table row set `==` the sections `dist/` carries, so a new section cannot ship without declaring its question). No baseline and no exemption list: a section is in scope because it carries a `<figure>`, which is what discharges the figure-less Limits sections. Since #53, Criterion 2's mechanical half joins it, described once here and cited by ID on each feature row it reaches: `test_no_standfirst_repeats_its_findings_figures` (a standfirst and its finding share no number token that is not a four-digit calendar year, the year exempted as a regex class rather than as a list), `test_every_finding_states_a_finding` (every `.finding` body and every `figure.figure` accessible name clears `finding_shape_problems`, the four-rule floor lifted unchanged out of `test_accessibility.py`'s `test_every_chart_svg_states_a_finding` so the two surfaces the contract calls the same sentence are held to one predicate), `test_no_finding_runs_past_the_cap` (`FINDING_CHARS_MAX` = 220, longest surviving finding 193), `test_every_finding_sits_where_the_stylesheet_expects_it` (at most one finding per section, immediately after its standfirst, before the first figure — the shape `src/styles/global.css:82` selects and the one-to-one the audit table and the `ariaLabel` pairing both assume), and `test_the_criterion_two_audit_covers_every_finding` (the contract's Criterion 2 audit table row set `==` the sections carrying a finding, 22 of them). Zero with no baseline: #53 found ten violations and fixed all ten. This route contributes **no** findings and **no** figures, so all five are structurally silent here and the row is listed for the file-level coverage, not for a check that bites on `/`. #53 also fixed the Criterion 1 audit slice boundary, which ended at the next `## ` and would have swallowed the Criterion 2 table whole, passing on the union of the two row sets. | `npx astro check`, `npm run build`, then against `dist/index.html`: `<h1>`=1, `id="one-picture"`=0; the pure-static set `client:` / `astro-island` / `aria-live` / `class="chart"` / `<figure` / `<svg` / `class="term` all **0**; `class="toc"`=1 and `data-section=`=**9** (4 sections × 2 lists, plus the one literal in `sectionSpy()`'s own selector string); the five cards' `href="/income-tax/{economy,households,government,sources,glossary}"` each ≥1 with `dist/{p}/index.html` present, the unbased forms and `href="http` both 0; `6 sections` / `7 sections` / `12 sections` = 1 each (derived from `routeSections`, not typed); `Units\.` / `Note\.` / `Source\.` / `View as table` / `Real FY2025` / `not to be persuaded of it` = 1 each; `grep -oiE 'primary source\|secondary source\|tier'` no match (#57 boundary); `.apparatus .lead` present in `dist/_astro/*.css`. Leak scan `grep -oE '\$[0-9]\|[0-9]+(\.[0-9]+)?%\|\b(19\|20)[0-9]{2}\b\|trillion\|billion'` returns only `0.5%`, `49.5%` and `50%`×3, every one of them from `BaseLayout`'s `sectionSpy()` comment and `rootMargin`, present identically on every route — **zero** matches from this page's own content | **NOT EXECUTED.** No browser pass has been run against the front door: the scroll-spy marking its four entries at 1440×900 and 390×844, the nav-bar panel mirroring them, and each card link landing on its route are all unverified in a real engine. What *is* proved from the served bytes rather than a browser: tab order is DOM order with no positive `tabindex` anywhere — skip link → nav-bar trigger → the visible nav set (panel below 62rem, rail above; the other is `display: none`) → its four contents links → the five card links → the two prose links in §4; and with scripting off every card link is a real `<a href>`, both contents lists are in the HTML, and nothing is marked. Five negative tests executed 2026-08-26 and each observed to bite, then reverted: renaming a `<section id>` and reordering two `sections` entries each fail `test_every_contents_anchor_is_addressable_by_the_spy`; an `<h3>` directly under the `<h1>` fails `test_heading_levels_do_not_skip`; `aria-live="polite"` on the cards list fails `test_live_regions_do_not_outnumber_charts` (0 charts ⇒ a budget of 0); dropping one card's `${base}` makes the unbased-href grep return 1. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. The Criterion 1 checks #52 added were proved to bite on 2026-08-26, five negative tests executed and reverted with `git status --porcelain` empty afterwards: relabelling HH-6's closing `<p class="prose">` fails `test_every_section_with_a_figure_answers_after_it` on that section; moving Economy section 1's standfirst below its `<Figure>` fails `test_every_section_with_a_figure_states_its_question_first` on `economy#one-picture`; copying `government#structural-gap`'s finding text into its standfirst fails `test_no_standfirst_preempts_its_finding` at overlap 1.000; renaming an `<h2>` to contain "panels" fails `test_no_section_heading_names_the_charts_construction`; deleting the `/government | by-state` row from the Criterion 1 audit table fails `test_the_criterion_one_audit_covers_every_section`, naming the missing section. **The human half of Criterion 1 is NOT EXECUTED** — Checklist item 8 in `contracts/prose.md`, which asks whether each standfirst poses a question a reader could have asked, whether the closing prose answers that question rather than describing the chart again, and whether each heading names a question or a claim rather than the section's variables. The last of those is invisible to any word list, and no word list was invented to fake it. | no browser-executed manual pass (above); no automated coverage of the rendered scroll-spy on this page specifically — the mechanism is `BaseLayout`'s and is manually proved on `/economy`, `/households` and `/government` only |

## Economy route

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| ECO-1 | `test_real_gdp_is_positive_in_every_fiscal_year`, `test_nominal_gdp_fy1995_matches_the_budget_route_denominator`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build`, `grep -o '<svg' dist/economy/index.html` (no-JS render proof) | 390px width and greyscale checks (`.claude/plans/issue-12.md` V-16, V-17) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio; 390px with scripting on **FAIL** — #64, #66, #73; 390px with scripting off **FAIL** — the `<noscript>` type mitigation never applies, #78. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `RealGdpLogScale.tsx`'s focus/readout interaction; proved only by the pytest data invariants plus the manual checks |
| ECO-2 | `test_output_per_hour_and_median_income_share_1984_to_2024`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. This section's shared `100` is gone: the 1984 = 100 base is stated once, in the standfirst, and the finding gives the two index values alone. | `npx astro check`, `npm run build` | same 390px/greyscale checks as ECO-1 — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio (worst co-occurring pair 1.95:1, carried by in-plot end labels and table columns); 390px FAILs #64, #66, #73, #78. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `GrowthAndShadow.tsx`'s interaction |
| ECO-3 | `test_unemployment_peak_over_actuals_is_fy1983`, `test_participation_peak_over_actuals_is_fy2000`, `test_fy2020_unemployment_is_a_fiscal_year_average_not_a_monthly_peak`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build` | same 390px/greyscale checks as ECO-1 — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio (worst co-occurring pair 1.95:1, carried by in-plot end labels and table columns); 390px FAILs #64, #66, #73, #78. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `WhoWorks.tsx`'s two-panel interaction |
| ECO-4 | `test_rate_series_start_at_their_documented_first_year`, `test_no_rate_series_is_negative_and_the_minima_are_near_zero`, `test_cpi_inflation_is_negative_in_fy1955_and_fy2009`, `test_nice_extent_zero_anchors_a_non_negative_series`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #52, the same file carries Criterion 1's mechanical half over every `<section id>` on this route: `test_every_section_with_a_figure_states_its_question_first`, `test_every_section_with_a_figure_answers_after_it` (split on the **last** `</figure>`, so a two-figure section is not passed by a paragraph sitting between its charts), `test_no_standfirst_preempts_its_finding` (number-token Jaccard strictly below `PREEMPTION_CEILING` = 0.5), `test_no_section_heading_names_the_charts_construction`, and `test_the_criterion_one_audit_covers_every_section` (the contract's audit table row set `==` the sections `dist/` carries, so a new section cannot ship without declaring its question). No baseline and no exemption list: a section is in scope because it carries a `<figure>`, which is what discharges the figure-less Limits sections. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. The shared `4.4` is gone from the standfirst and the 236-character finding is down to 111, one claim about which of the two peaks came first; the current levels and the near-zero decade moved into the closing prose, which gained a paragraph. | `npx astro check`, `npm run build`, `grep -o '<svg' dist/economy/index.html` (no-JS render proof) | tab/live-region parity (M1), table contents, 390px width (M4/M5) and greyscale (M6) checks in `.claude/plans/issue-13.md` — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: M1 keyboard **executed**, FAILs #69, #71; M4/M5 **FAIL** — #64, #66, #73, #78; M6 greyscale **PASS** with a computed per-panel worst-pair luminance ratio — this figure holds the route's tightest pair, 1.03:1 between the 3-month bill and the 10-year note, carried by in-plot end labels and three table columns; live-region *announcement* parity is a screen-reader property, the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80**. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. The Criterion 1 checks #52 added were proved to bite on 2026-08-26, five negative tests executed and reverted with `git status --porcelain` empty afterwards: relabelling HH-6's closing `<p class="prose">` fails `test_every_section_with_a_figure_answers_after_it` on that section; moving Economy section 1's standfirst below its `<Figure>` fails `test_every_section_with_a_figure_states_its_question_first` on `economy#one-picture`; copying `government#structural-gap`'s finding text into its standfirst fails `test_no_standfirst_preempts_its_finding` at overlap 1.000; renaming an `<h2>` to contain "panels" fails `test_no_section_heading_names_the_charts_construction`; deleting the `/government | by-state` row from the Criterion 1 audit table fails `test_the_criterion_one_audit_covers_every_section`, naming the missing section. **The human half of Criterion 1 is NOT EXECUTED** — Checklist item 8 in `contracts/prose.md`, which asks whether each standfirst poses a question a reader could have asked, whether the closing prose answers that question rather than describing the chart again, and whether each heading names a question or a claim rather than the section's variables. The last of those is invisible to any word list, and no word list was invented to fake it. | no automated JS coverage of `PricesAndRates.tsx`'s two-panel interaction; proved only by the pytest data invariants plus the manual checks |
| ECO-5 | `test_wage_and_profit_share_are_gdp_shares_and_do_not_sum_to_100`, `test_fy2020_share_moves_are_denominator_artefacts`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #52, the same file carries Criterion 1's mechanical half over every `<section id>` on this route: `test_every_section_with_a_figure_states_its_question_first`, `test_every_section_with_a_figure_answers_after_it` (split on the **last** `</figure>`, so a two-figure section is not passed by a paragraph sitting between its charts), `test_no_standfirst_preempts_its_finding` (number-token Jaccard strictly below `PREEMPTION_CEILING` = 0.5), `test_no_section_heading_names_the_charts_construction`, and `test_the_criterion_one_audit_covers_every_section` (the contract's audit table row set `==` the sections `dist/` carries, so a new section cannot ship without declaring its question). No baseline and no exemption list: a section is in scope because it carries a `<figure>`, which is what discharges the figure-less Limits sections. | `npx astro check`, `npm run build` | same M1, M4/M5 and M6 checks as the other Economy-route figures — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio (5.54:1); 390px FAILs #64, #66, #73, #78. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. The Criterion 1 checks #52 added were proved to bite on 2026-08-26, five negative tests executed and reverted with `git status --porcelain` empty afterwards: relabelling HH-6's closing `<p class="prose">` fails `test_every_section_with_a_figure_answers_after_it` on that section; moving Economy section 1's standfirst below its `<Figure>` fails `test_every_section_with_a_figure_states_its_question_first` on `economy#one-picture`; copying `government#structural-gap`'s finding text into its standfirst fails `test_no_standfirst_preempts_its_finding` at overlap 1.000; renaming an `<h2>` to contain "panels" fails `test_no_section_heading_names_the_charts_construction`; deleting the `/government | by-state` row from the Criterion 1 audit table fails `test_the_criterion_one_audit_covers_every_section`, naming the missing section. **The human half of Criterion 1 is NOT EXECUTED** — Checklist item 8 in `contracts/prose.md`, which asks whether each standfirst poses a question a reader could have asked, whether the closing prose answers that question rather than describing the chart again, and whether each heading names a question or a claim rather than the section's variables. The last of those is invisible to any word list, and no word list was invented to fake it. | no automated JS coverage of `LaborAndCapital.tsx`'s interaction |
| ECO-6 | — (prose-only section, no chart or new field). Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build`, `grep -o 'id="limits"'`/`grep -o 'href="/income-tax/households/"'` on `dist/economy/index.html` | none needed — no chart, no interaction. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | none |

## Government route

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| GOV-1 | `test_debt_chart_uses_the_shared_unit_toggle`, `test_section_1_spells_out_its_trillions`, `test_no_dollar_axis_labels_a_zero_tick_in_billions` (all `test_chart_vocabulary.py`); `test_every_figure_server_renders_its_chart_svg`, `test_government_section_1_renders_its_whole_apparatus_without_scripting` (`test_accessibility.py`, #36 — SSR presence of the chart `<svg>` and of §1's whole no-JS apparatus, run against the `npm run build` output). Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. The shared `40` between this section's standfirst and its finding is gone; the standfirst keeps the 105% to 124% of GDP series as the 'measured against what' half of the setup. | `npx astro check`, `npm run build`, `npm run test:unit` (`format.test.ts` — `tick`'s zero case and `trillionsLong`'s byte-exactness against the `fmtFull` #35 deleted); `grep -o '<svg' dist/government/index.html \| wc -l` → 14, all 14 `class="chart"` | keyboard walk of the two-option toggle, and the live readout at 390px. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | point focus/hover geometry — which datum a given pointer position or Tab press selects — is unproven; the source-level and built-HTML guards fix the *text* each state announces, not the hit-testing that chooses the state |
| GOV-2 | `test_crossing_date_is_reconciled_not_competing`, `test_foreign_share_always_carries_its_denominator`, `test_public_split_is_keyed_to_its_denominator`, `test_section_2_uses_no_party_colours`, `test_federal_reserve_absent_from_rendered_section_2`, `test_tic_revision_note_is_carried` (rebased by #54 onto the release month and the two TIC readings), `test_curated_snapshots_expose_their_as_of` (rebased by #54: `debt_holders` now asserts `mode == "mixed"` and exactly one `mixedVintage(` call in the manifest, `debt_maturity` keeps `curated`). Since #54, the TIC fetch: `test_foreign_holdings_come_from_tic`, `test_the_two_denominators_reconcile`, `test_no_emitted_figure_is_sourced_to_a_news_outlet`, and seven parser unit tests over an inline two-block slice of `mfhhis01.txt` (`test_tic_parser_selects_the_pinned_months_column`, `..._rejects_a_missing_vintage`, `..._rejects_a_malformed_vintage`, `..._refuses_a_block_with_a_row_missing` (parametrized over a country and the grand total), `..._refuses_a_cell_that_is_not_a_number`, `..._refuses_a_body_it_could_not_parse_at_all`, `test_tic_fetches_through_the_one_http_core`), which need no network. **The six new build-time guards are each proved to bite** by an automated negative test rather than a manual note (#38's shape): `test_the_guards_pass_on_what_is_actually_published` fixes the clean baseline, then `test_g1_bites_a_malformed_or_disagreeing_vintage`, `test_g2_bites_a_release_nobody_pinned`, `test_g3_bites_a_shifted_country_row`, `test_g4_bites_a_denominator_moved_without_the_other`, `test_g5_bites_a_grand_total_that_stops_corroborating`, `test_g_china_bites_a_fetched_value_that_left_its_resolution_behind` and `test_g6_bites_an_outlet_reappearing_in_an_emitted_file` each monkeypatch `validate.DATA_DIR` to a mutated copy of `src/data` and assert the specific failure message. `uv run pytest -k bites` is the one command that proves the set. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build` (`mixedVintage` throws at build time if `debtHolders._meta.refresh.mode` is not `mixed`, or if `_meta.provenance.vintage` is missing — changed from `curatedVintage`/`curated` by #54, and still the hard gate that makes "this output is honestly described" un-skippable), `cd pipeline && uv run python build.py --tier monthly --dry-run` (guards G1-G6 plus the China bound, 0 drift, 0 failures) | M1 keyboard **executed 2026-08-24** (Chrome 151; FAILs #69, #71), M4/M5 390px **executed** (JS on #64/#66/#73/#74, JS off #78), M6 greyscale **executed 2026-08-26** — PASS with a computed per-panel luminance ratio, M2 screen reader **NOT EXECUTED, human + assistive technology required (#80)**; results in `docs/contracts/accessibility.md` § Manual pass results. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `DebtHolders.tsx`'s bar/connector geometry; "View as table" (M6) could not be verified against `dist/government/index.html` because Radix `Collapsible.Content` does not render its children in the closed-state SSR output (also true of the pre-existing `DebtChart.tsx` table, not a regression here) — table correctness (units, `no data` cells) is proved by the component's row-building logic and by `TableView`'s own `?? 'no data'` contract, not by a static grep of the built HTML. **Superseded by A11Y-1's D1 fix**: `TableView` now renders via a native `<details>`/`<summary>` element, present in server-rendered HTML regardless of scripting, so the Radix-SSR gap noted above no longer applies — re-verify M6 with a static grep against `dist/government/index.html` |
| GOV-3 | `test_section_2_uses_no_party_colours` (covers both islands), `test_maturity_instruments_are_not_an_exhaustive_partition` and `test_maturity_percentages_are_never_derived_from_amounts` (both rebased by #56 onto the fetched marketable total: the first now asserts a gap of at least $2.0T rather than a tolerance of $0.01T, the second that the published bills share RECONCILES within 0.5pp rather than disagrees), `test_maturity_history_is_not_charted`, `test_curated_snapshots_expose_their_as_of` (now covers both mixed datasets). #56 adds `pytest -k mspd`: five negative mutation tests over a copy of `src/data` proving each guard bites by its own failure message (a malformed or disagreeing statement month, a statement nobody pinned, `marketable_total_t` restored to the published `28.0`, a bills share moved to 24, a class set missing TIPS), one proving the Peterson attribution cannot return to an emitted file, their clean-inverse `test_the_mspd_guards_pass_on_what_is_actually_published`, and six parser unit tests over `lib/mspd.py` covering a window that resolves to two month-ends, a narrowed class set, an empty response, an unparseable amount, the total being summed over every class rather than the three drawn, and the half-open month window rolling the year in December. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. This section held the site's one `figure.figure` accessible name that failed the finding-shape floor, opening 'Chart showing US marketable Treasury debt split by instrument type'. It was invisible to `test_every_chart_svg_states_a_finding`, which visits `svg.chart` names only and never sees a name this island's figure carries; extending the same floor to `figure.figure` names is what caught it. | `npx astro check`, `npm run build` | M1 keyboard **executed 2026-08-24** (Chrome 151; FAILs #69, #71), M4/M5 390px **executed** (JS on #64/#66/#73/#74, JS off #78), M6 greyscale **executed 2026-08-26** — PASS with a computed per-panel luminance ratio, M2 screen reader **NOT EXECUTED, human + assistive technology required (#80)**; results in `docs/contracts/accessibility.md` § Manual pass results. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `DebtMaturity.tsx`'s band/marker geometry; "View as table" (M6) has the same Radix-SSR gap noted under GOV-2, now also superseded by A11Y-1's D1 fix |
| GOV-4 | `test_outlay_components_sum_to_total_in_every_unit_family`, `test_party_control_is_null_outside_fy1995_2025`, `test_net_mandatory_is_positive_in_every_year`, `test_every_unit_family_covers_the_full_span`, `test_surplus_years_are_positive_deficit_values`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #52, the same file carries Criterion 1's mechanical half over every `<section id>` on this route: `test_every_section_with_a_figure_states_its_question_first`, `test_every_section_with_a_figure_answers_after_it` (split on the **last** `</figure>`, so a two-figure section is not passed by a paragraph sitting between its charts), `test_no_standfirst_preempts_its_finding` (number-token Jaccard strictly below `PREEMPTION_CEILING` = 0.5), `test_no_section_heading_names_the_charts_construction`, and `test_the_criterion_one_audit_covers_every_section` (the contract's audit table row set `==` the sections `dist/` carries, so a new section cannot ship without declaring its question). No baseline and no exemption list: a section is in scope because it carries a `<figure>`, which is what discharges the figure-less Limits sections. | `npx astro check`, `npm run build` | M1–M12 in `.claude/plans/issue-2.md` (unit-sum spot check, control strip absent pre-1995, keyboard inspection parity, GDP-view clipping, no-JS render, 390px width, greyscale). **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. The Criterion 1 checks #52 added were proved to bite on 2026-08-26, five negative tests executed and reverted with `git status --porcelain` empty afterwards: relabelling HH-6's closing `<p class="prose">` fails `test_every_section_with_a_figure_answers_after_it` on that section; moving Economy section 1's standfirst below its `<Figure>` fails `test_every_section_with_a_figure_states_its_question_first` on `economy#one-picture`; copying `government#structural-gap`'s finding text into its standfirst fails `test_no_standfirst_preempts_its_finding` at overlap 1.000; renaming an `<h2>` to contain "panels" fails `test_no_section_heading_names_the_charts_construction`; deleting the `/government | by-state` row from the Criterion 1 audit table fails `test_the_criterion_one_audit_covers_every_section`, naming the missing section. **The human half of Criterion 1 is NOT EXECUTED** — Checklist item 8 in `contracts/prose.md`, which asks whether each standfirst poses a question a reader could have asked, whether the closing prose answers that question rather than describing the chart again, and whether each heading names a question or a claim rather than the section's variables. The last of those is invisible to any word list, and no word list was invented to fake it. | no automated JS coverage of `BudgetChart.tsx` — the stacking, strip and inspector logic are proved only by the pytest data invariants above plus the manual checks; the `npm run test:unit` runner added in #34 covers pure modules only and does not render this island, so the gap is open. Since #33 this module no longer implements the laws-to-splits join — it calls the shared `src/components/laws/join.ts`, guarded at the source level by `test_the_laws_to_splits_join_has_exactly_one_implementation`; the unmatched-law throw is still unexercised at runtime because all 23 laws join |
| GOV-5 | `test_section5_revenue_and_outlay_means_hold`, `test_section5_surplus_band_is_exactly_fy1998_2001`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build` | M1, M2, M4, M5, M6, M7 in `.claude/plans/issue-5.md` (surplus band reads in greyscale, FY2020 does not clip in any unit view, keyboard/live-region parity, table view, no-JS render, 390px width). **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `StructuralGap.tsx` — the fill segmentation, hatch pattern and per-datum focus logic are proved only by the pytest data invariants above plus the manual checks |
| GOV-6 | `test_section6_mandatory_growth_is_quoted_on_the_net_basis`, `test_nice_extent_zero_anchors_a_non_negative_series`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build` | M2, M4, M5, M6, M7 in `.claude/plans/issue-5.md` (FY2020 does not clip in any unit view, keyboard/live-region parity, table view, no-JS render, 390px width). **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `VotedAndNot.tsx` — the net-basis computation and FY2015 marker are proved only by the pytest data invariant above plus the manual checks |
| GOV-7 | `test_section7_net_interest_share_of_deficits_is_39_percent`, `test_section7_series_low_is_not_the_fy2015_trough`, `test_net_interest_series_low_is_fy2003`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build` | M3, M4, M5, M6, M7, M8 in `.claude/plans/issue-5.md` (FY2003 bar plainly non-zero, keyboard/live-region parity, table view, no-JS render, 390px width, cross-link scrolls to `#how-old`). **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `NetInterest.tsx` — the zero-anchored bar geometry and trough/series-low vocabulary are proved only by the pytest data invariants above plus the manual checks |
| GOV-8 | `test_tcja_verified_source_is_the_clerk_record_alone` (#56 — the TCJA cites the House Clerk roll call alone, Ballotpedia appears nowhere in `budget.json`, and the four verified counts plus `rollcall.house: 698` are asserted beside the string so the edit cannot drift into the numbers; 698 vs RC699 is Voteview's index against the Clerk's and must not be reconciled), `test_every_law_joins_to_a_counted_split`, `test_filter_totals_render_to_the_published_two_places`, `test_the_two_1997_laws_carry_no_score_but_do_carry_votes`, `test_cares_house_cell_has_no_countable_vote`, `test_vp_tiebreak_laws_are_exactly_the_three_named`, `test_narrowest_chamber_margin_is_defined_for_every_law`, `test_deficit_share_exists_for_every_enactment_fiscal_year`, `test_section_8_no_longer_claims_classified_composition`, `test_no_document_still_calls_vote_composition_classified`, `test_law_split_totals_round_the_sum_not_the_displays`, `test_check_laws_rejects_a_drifted_split_total`, `test_the_laws_to_splits_join_has_exactly_one_implementation`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build`, `uv run python build.py --tier monthly`, `uv run python build.py --tier monthly --dry-run` | M1–M14 in `.claude/plans/issue-3.md` (filter/totals spot checks, basis toggle, CARES/1997/50-50 rendering, margin and score sort stability, keyboard-only Select/sort/selection, no-JS render, 390px width, greyscale). **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | `npm run test:unit` (added in #34) renders no React island, so `LawExplorer.tsx`'s filter, sort, selection and basis-toggle logic is proved only by the pytest data invariants above (`derive.ts`'s pure functions are mirrored in Python for the pipeline tests, not unit-tested directly in TS) plus the manual checks. Since #33 the laws-to-splits join is single-sourced in `src/components/laws/join.ts`, shared with GOV-9; `test_the_laws_to_splits_join_has_exactly_one_implementation` is a source-level guard on that (it cannot exercise the join at runtime — `test:unit` covers pure modules, not islands) |
| GOV-9 | `test_attribution_both_breakdowns_reconcile_to_the_same_total`, `test_attribution_coalition_totals_match_the_published_finding`, `test_attribution_president_totals_match_the_published_finding`, `test_attribution_gross_and_net_differ_by_the_reductions`, `test_attribution_excludes_the_two_1997_laws`, `test_attribution_party_line_side_is_counted_not_classified`, `test_attribution_every_law_joins_to_a_counted_split`, `test_the_laws_to_splits_join_has_exactly_one_implementation`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. The 240-character finding, eight figures across two attributions, is down to 150 and one claim. The standfirst gave up the $16.75 trillion so the finding could make it the claim, which is the resolution the issue's own edge case proposed. | `npx astro check`, `npm run build` (runs `aggregate.ts`'s import-time reconciliation invariant) | M1–M8 in `.claude/plans/issue-4.md` (keyboard tab switch + bar traversal, screen-reader announcement, no-JS render, 390px width, greyscale, table disclosure totals, reduced-motion, contrast). **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `AttributionSplit.tsx` — the bar geometry, tab wiring and live-region text are proved only by the pytest reconciliation tests above plus the manual checks; the `npm run test:unit` runner added in #34 covers pure modules only and does not render this island, so the gap is open. Since #33 this module no longer implements the laws-to-splits join — it calls the shared `src/components/laws/join.ts`, guarded at the source level by `test_the_laws_to_splits_join_has_exactly_one_implementation`; the unmatched-law throw is still unexercised at runtime because all 23 laws join |
| GOV-10 | `test_revenue_components_sum_in_the_gdp_and_share_families`, `test_miscellaneous_revenue_is_never_zero`, `test_revenue_series_is_contiguous_and_has_no_null_fields`, `test_customs_is_the_fastest_growing_revenue_line`, `test_oecd_average_is_flagged_and_the_country_list_is_a_selection`, `test_oecd_and_federal_revenue_are_marked_as_different_scopes` (plus the pre-existing `test_revenue_components_sum_to_total`), `test_nice_extent_zero_anchors_a_non_negative_series`, `uv run python build.py --tier monthly --dry-run` (0 validation failures). Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build` | M1–M7 in `.claude/plans/issue-7.md` (no-JS render, 390px width, keyboard sweep parity, greyscale, view-toggle consistency, table units, screen-reader announce order) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: no-JS render **executed**; 390px **FAIL** — #62, #64, #66, #73, #78; keyboard parity **executed**, FAILs #69, #71, #72; greyscale **PASS (note)** — this chart holds the site's worst co-occurring pair, `--rev-pr` against `--rev-eg` at **1.00:1**, non-adjacent in the stack, tightest adjacent band 1.44:1, four of seven sources labelled in-plot and all seven carried as table columns; announce order is **NOT EXECUTED — human plus an assistive technology required, #80**. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | the axis-domain rule these charts share is covered by `npm run test:unit` (`src/components/charts/scales.test.ts`, added in #34 — `RevenueChart`'s nominal view is one of the two axes it zero-anchors); there is still no automated JS coverage of `RevenueChart.tsx`/`OecdChart.tsx` interactions — the stacking, focus/readout and dot-plot logic are proved only by the pytest data invariants above plus the (unrun) manual checks, since `test:unit` renders no island. Since #33 this module no longer implements the laws-to-splits join — it calls the shared `src/components/laws/join.ts`, guarded at the source level by `test_the_laws_to_splits_join_has_exactly_one_implementation`; the unmatched-law throw is still unexercised at runtime because all 23 laws join |
| GOV-11 | 11 tests (`pytest -k state`): `test_states_rockefeller_limitation_is_stated_everywhere_it_was_ruled` (#56 — the dated ruling is present in `state-data.md`, the six echoes of the limitation are all present, and `rockefeller_bop` still carries `cited_in_prose_only: true`; a ruling with no guard is a sentence a later sweep removes from one file and leaves in five), plus 51 in-grid jurisdictions, DC flagged and colour-excluded, territory give/get asymmetry, no derived field silently zero, give/get same fiscal year, jurisdiction give sums reconcile to the national total, ratio null-or-positive, tax-mix shares in `[0,100]` or null, `not_levied` vs missing distinct, no party-colour token in the section source. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build`, plus `check_schema`/`check_states` in `lib/validate.py` (JSON Schema + 8 reconciliation invariants, gates every `--tier monthly` run) | keyboard/live-region parity across all 51 tiles into the sortable table (Enter/Space to sort), colour never carries meaning alone, 390px legibility, JS-disabled render, focus-ring visibility — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: keyboard walk **executed**, FAILs #69, #71; live-region *announcement* parity (one live region per chart, not per data point) is a screen-reader property and is **NOT EXECUTED — human plus an assistive technology required, #80** — the 2026-08-24/26 pass covered the Tab-focusable half only and this row must not be read as claiming otherwise; 390px **FAIL** — #63, #74, #78; JS-disabled render **executed**, all 13 tables present with scripting off; focus ring **executed** in Chrome 151 and WebKit 26.5 — a ring paints, but its 1.5px thickness fails WCAG 2.2 Focus Appearance (#75), and Safari.app itself is **NOT EXECUTED — human plus an assistive technology required, #80**. Greyscale **PASS (note)**, which is where *colour never carries meaning alone* was checked and where it holds: the cartogram's adjacent scale steps separate by 1.00:1, with direction carried by a `+`/`−` glyph on every tile and magnitude by the table; the state tax-mix bar separates by 1.44:1 with no legend and no in-plot label, its three segments named only inside the disclosure table; every colour-coded category is therefore also reachable as text — the state postal code on each tile, the three category names as columns of the tax-mix disclosure table — so no category is distinguished by hue alone; structural proxies for all four are in `.claude/plans/issue-14.md` Definition of done 8/11/12. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no browser-driven test exists anywhere in this repo (consistent with GOV-1/GOV-4); the tile grid's 51-target keyboard walk and the sortable table's Enter/Space activation are unverified by anything but the manual pass |
| GOV-12 | `test_crisis_years_are_a_third_of_all_borrowing`, `test_deficit_debt_gap_matches_the_curated_resolution`, `test_limits_section_quotes_the_curated_deficit_debt_gap`, `test_limits_section_does_not_call_the_votes_classified` (unchanged by #39 — limit 6 is worded without the `classif` stem so the existing whole-section guard passes as written; the test was **not** narrowed to make room for the copy), `test_exactly_one_chamber_vote_is_absent`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build` | M1–M5 in `.claude/plans/issue-8.md` (no-JS render, 390px width, tab order, greyscale, screen reader) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: no-JS render **executed**, every `<details>` table present with scripting off; 390px with scripting off **FAIL** — the `<noscript>` type mitigation never applies, #78; tab order **executed**, FAILs #69, #71, #72; greyscale **PASS** with a computed per-panel worst-pair luminance ratio; the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80**. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no JS runner, but §12 ships no JS so this is a non-gap. **Nothing asserts the number of `<li>` in `<ol class="limits">`** — the heading says "Six" and the list carries six as of #39, but the two could drift apart without any test noticing; the count is checked by hand against `dist/government/index.html` at review time |

## Households route

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| HH-1 | `test_median_income_reproduces_the_published_figures`, `test_1947_has_a_gini_but_no_median_income`, `test_series_start_years_match_the_declared_coverage`, `test_absent_observations_are_null_not_zero`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. The shared `28.1` is gone: the standfirst now sets the chart up and the finding keeps the figure. This also retired the `standfirst|Real median household income rose #% in three decades, but t` fingerprint from `KNOWN_DASH_DEBT`, because the rewritten sentence took its em dash with it. | `npx astro check`, `npm run build`, static-render greps against `dist/households/index.html` (verification 5 in `.claude/plans/issue-9.md`) | M1, M2, M6–M9 in `.claude/plans/issue-9.md` (slider keyboard operation, datum focus parity, table units, 390px width, greyscale) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results. Scope correction: **no slider renders** — the site has zero `<input>` and zero `<select>` elements, so the slider keyboard item has no consumer. Datum focus parity **executed**, FAILs #69, #71; 390px **FAIL** — #64, #66, #73, #78; greyscale **PASS** with a computed per-panel worst-pair luminance ratio (single series, nothing carried by colour); the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80**. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `MedianIncome.tsx` or `YearRange.tsx` interactions; of the manual items above only the screen-reader half is still unexecuted (#80), the rest were run in a browser |
| HH-2 | `test_family_gini_reproduces_the_published_figures`, `test_cbo_top1_share_is_two_published_points_not_a_series`, `test_gini_is_labelled_as_families`, `test_absent_observations_are_null_not_zero`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. The shared `1`, from 'top 1%' in both elements, is gone; the standfirst names the series by its two publishers and their two shapes instead. | `npx astro check`, `npm run build`, static-render greps against `dist/households/index.html` | M1, M2, M3–M6, M9, M10 in `.claude/plans/issue-9.md` (empty-range state, no-connector check, table units, greyscale, VoiceOver) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: greyscale **PASS** with a computed per-panel worst-pair luminance ratio (single series per panel); 390px **FAIL** — #64, #66, #73, #78; the VoiceOver item is the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80**. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `HouseholdSpread.tsx`; of the manual items above only the VoiceOver half is still unexecuted (#80) |
| HH-3 | `test_bracket_history_reproduces_curated_top_rates`, `test_bracket_history_adjustment_years_are_documented`, `test_bracket_thresholds_carry_both_nominal_and_constant_dollars`, `test_filing_statuses_are_not_projected_backwards`, `test_bracket_count_runs_from_two_in_1988_to_fiftysix_in_1918`, `test_bracket_history_absent_values_are_null_not_zero`; `check_bracket_history` at the build validation gate. Since #55, the top-rate series carries its own primary-source guard: `test_top_rates_match_the_soi_table_23_anchor` (all 106 overlapping years of `curated/top_rates.yaml` equal IRS SOI Historical Table 23's highest-bracket column, and the anchor covers 1913-2018 with no gaps -- the no-gaps half is what stops a footnote-prefixed cell such as 1954's `[19] 91.0` being silently dropped and shrinking the check's reach), and the negative `test_the_soi_anchor_check_rejects_a_top_rate_that_drifts_from_it`, which drifts 1944, 1913 and 2018 in turn and requires a single failure naming the year and both values, plus a year missing from the published series and a gap in the anchor. `check_top_rates_anchor` runs at the build validation gate UNCONDITIONALLY, not behind a tier gate, because it reconciles two curated files and needs no output (#37's lesson). Proved to bite on 2026-08-26: the check was neutered with an early `return` and the negative test failed with `expected exactly one failure for 1944, got []`, then passed again on restore. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. The 216-character finding, four characters under a cap that had not yet been set, is down to 97 and one claim; its `<Figure ariaLabel>` moved with it, and the fingerprint `finding|The # top bracket started at $# about $# million in # dollar` left `KNOWN_DASH_DEBT`. | `npx astro check`, `npm run build`; static-render greps in `.claude/plans/issue-10.md` V6 (both sections present, `scaleLog` used, SVGs server-rendered) | M1 (Tab-focus/live-region parity), M2 ("View as table" + units), M3 (`no data` not `0` pre-1949/1952), M4 (no-JS render), M5 (390px legibility), M6 (greyscale), M7 (log-axis label), M8 (VoiceOver) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: M1 **executed**, FAILs #69, #71; M4 **executed**, table present with scripting off; M5 **FAIL** — #64, #66, #73, #78; M6 **PASS (note)** — worst co-occurring pair 1.31:1, but the three panels are separate plots each with its own in-plot title; M8 is **NOT EXECUTED — human plus an assistive technology required, #80**. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `BracketHistory.tsx` interactions; of M1-M8 only M8 (VoiceOver) is still unexecuted (#80) |
| HH-4 | `test_cbo_effective_rates_are_anchor_points_not_a_series`, `test_cbo_effective_rates_basis_names_payroll_tax`; `check_cbo_effective_rates` at the build validation gate. The statutory half of this section's comparison is `curated/top_rates.yaml`, which also supplies `income_inequality.top`, so #55's `check_top_rates_anchor` and its two tests (named in full on HH-3) cover this row's top-rate series as well -- one guard over a two-route file, including the 2024 = 37.0 and 1944 = 94.0 spot checks `check_bracket_history` already made. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. The 253-character finding, the longest on the site, is down to 156 and one claim, and the fingerprint `finding|The top statutory rate fell from #% in # to #% in # Over the` left `KNOWN_DASH_DEBT`. The `svg.chart` label for the same figure is generated by `StatutoryVsEffective.tsx:97` and is #102's; only the `figure.figure` name moved here, so the two still diverge. | `npx astro check`, `npm run build`; static-render greps (`Nobody pays the top rate on their whole income`, `includes payroll tax`) | M4 (no-JS render), M6 (greyscale, marker shapes distinguish the five groups without colour), M8 (VoiceOver) — **executed 2026-08-24 / 2026-08-26** (Chrome 151 at 1440x900 and 390x844, WebKit 26.5); per-check results in `docs/contracts/accessibility.md` § Manual pass results: M4 **executed**, table present with scripting off, but 390px with scripting off **FAIL** — the `<noscript>` type mitigation never applies, #78; M6 **PASS, confirmed in greyscale** — the five income groups are drawn as circle, square, triangle, diamond, × and +, with a shape legend, so the 1.06:1 colour separation carries nothing on its own; M8 is the screen-reader half **NOT EXECUTED — human plus an assistive technology required, #80**. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `StatutoryVsEffective.tsx` interactions; the no-connector (anchor-point, not line) rendering is proved by manual inspection of the 2026-08-26 greyscale render, which shows the six groups as unconnected shape markers |
| HH-5 | `test_top1_income_share_is_present_and_paired`, `test_percentile_groups_are_nested_not_a_partition`, `test_unpublished_group_cells_are_absent_not_zero`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #53, Criterion 2's mechanical half reaches this row too, described in full on INTRO-1: `test_no_standfirst_repeats_its_findings_figures`, `test_every_finding_states_a_finding`, `test_no_finding_runs_past_the_cap`, `test_every_finding_sits_where_the_stylesheet_expects_it` and `test_the_criterion_two_audit_covers_every_finding`, plus this section's row in the contract's Criterion 2 audit table. The shared `1`, `20.6` and `38.4` are gone and the finding is one group's claim rather than three groups' six figures. This section carries two figures under one finding, so the Criterion 2 audit row names which of them the claim is checkable against. | `npx astro check`, `npm run build` | M1 (390px legibility), M2 (keyboard parity), M3 (greyscale), M4 (screen-reader pass over `WhoPays`, absent cells announce as "no data") in `.claude/plans/issue-11.md`. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no automated JS coverage of `WhoPays.tsx` / `Top1TaxShare.tsx` interactions — the nesting, absence and non-annual invariants are proved only by the pytest data checks above plus manual review |
| HH-6 | none new — reuses `check_revenue`'s FY2025 `g_pr`/`s_pr` assertions in `pipeline/lib/validate.py`, run via `uv run python build.py --tier monthly --dry-run`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. Since #52, the same file carries Criterion 1's mechanical half over every `<section id>` on this route: `test_every_section_with_a_figure_states_its_question_first`, `test_every_section_with_a_figure_answers_after_it` (split on the **last** `</figure>`, so a two-figure section is not passed by a paragraph sitting between its charts), `test_no_standfirst_preempts_its_finding` (number-token Jaccard strictly below `PREEMPTION_CEILING` = 0.5), `test_no_section_heading_names_the_charts_construction`, and `test_the_criterion_one_audit_covers_every_section` (the contract's audit table row set `==` the sections `dist/` carries, so a new section cannot ship without declaring its question). No baseline and no exemption list: a section is in scope because it carries a `<figure>`, which is what discharges the figure-less Limits sections. | `npx astro check`, `npm run build` | M1 (390px, end labels not overlapping), M2 (keyboard parity), M3 (greyscale) in `.claude/plans/issue-11.md`. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. The Criterion 1 checks #52 added were proved to bite on 2026-08-26, five negative tests executed and reverted with `git status --porcelain` empty afterwards: relabelling HH-6's closing `<p class="prose">` fails `test_every_section_with_a_figure_answers_after_it` on that section; moving Economy section 1's standfirst below its `<Figure>` fails `test_every_section_with_a_figure_states_its_question_first` on `economy#one-picture`; copying `government#structural-gap`'s finding text into its standfirst fails `test_no_standfirst_preempts_its_finding` at overlap 1.000; renaming an `<h2>` to contain "panels" fails `test_no_section_heading_names_the_charts_construction`; deleting the `/government | by-state` row from the Criterion 1 audit table fails `test_the_criterion_one_audit_covers_every_section`, naming the missing section. **The human half of Criterion 1 is NOT EXECUTED** — Checklist item 8 in `contracts/prose.md`, which asks whether each standfirst poses a question a reader could have asked, whether the closing prose answers that question rather than describing the chart again, and whether each heading names a question or a claim rather than the section's variables. The last of those is invisible to any word list, and no word list was invented to fake it. | no automated JS coverage of `PayrollBill.tsx`'s two-view toggle |
| HH-7 | none — prose only, no chart. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build` | manual review of the five numbered limits against the grep-pinned phrases in `.claude/plans/issue-11.md`. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | no chart, so no data-invariant test applies |

## Reference pages

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| REF-1 | `test_sources_doc_describes_counted_splits`, plus the whole of DATA-3 — since #39 the register is gated rather than merely rendered, so `test_check_sources_is_clean_against_the_real_tree` is also REF-1 coverage: it fails if a source this site cites is missing from the document this route publishes. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. **Eleven cases since #57**, beside the `test_check_sources_*` family: `test_every_registered_source_states_a_tier_from_the_vocabulary` and `test_every_registered_source_is_followable_or_carries_a_written_exemption` (population is every register entry, so entry twenty-five cannot ship untiered or unfollowable), `test_voteview_is_describable_without_calling_it_secondary` (the issue's own named acceptance test, which also asserts `party_splits`' `cites` is unchanged — a tier is not a licence to swap a source), `test_a_compilation_names_what_it_compiles` (#55's prose argument, now machine-checked), `test_sources_doc_states_the_same_tier_as_the_register` (rule I against the real tree), `test_the_cbo_table_9_citation_is_read_from_meta_not_typed`, `test_check_sources_tolerates_a_vintage_refresh_of_a_tiered_lead_in` (E10 — a refresh that moves both sides stays green) and **four negative tests, each executed and each asserting a failure**: no tier, a tier outside the vocabulary, an unargued `secondary`, and a document tier disagreeing with the register. A fifth negative test covers rule G in both its shapes, including the dangerous one — a bare `url_exempt: true`, which looks like a decision and carries none. | the `if (!entry) throw` guard in `src/pages/sources.astro`, plus a `grep` on `dist/sources/index.html` | M2 (390px width, table scroll containment) in `.claude/plans/issue-8.md` — **executed 2026-08-24 / 2026-08-26**, and **re-executed 2026-08-26 after #57** (headless Chromium at 390x844 against a local build, because the fix is not deployed yet); per-check results in `docs/contracts/accessibility.md` § Manual pass results: **PASS** — `/sources/` now measures `documentElement.scrollWidth` **390** against a 390px `clientWidth`, against 520 before, and the widest `<code>` span is 348px against 500px, with none of the 45 clipped and none set to ellipsis. **#79 closes as fixed-by-#57.** No table renders on this route, so scroll containment does not apply. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | none |
| REF-2 | none new, and none hand-written: the conformance suite collects `dist/**/index.html` by glob (`test_accessibility.py`, `PAGES = sorted(DIST.glob("**/index.html"))`), so ~20 per-page cases run against `dist/glossary/index.html` with no test-file edit — and, since **A11Y-6 (#47)**, the five `test_*term*` checks run there too and assert what that page must *not* have: `dist/glossary/index.html` carries **0** `.term` markers, so the shared IIFE returns on its first line and the glossary does not link to itself — including `test_every_contents_anchor_is_addressable_by_the_spy` (the `sections` prop must name real `main section[id]` elements in document order, which is what makes the A–Z letter grouping honest rather than a stub), `test_route_nav_and_contents_nav_are_separate_landmarks`, `test_no_page_repeats_an_id`, `test_heading_levels_do_not_skip` and `test_every_figure_server_renders_its_chart_svg` (vacuous here — the route renders zero `<figure>`). `test_nav_bar_mirrors_every_route_and_section` and `test_every_contents_anchor_is_addressable_by_the_spy` are also the proof that extracting `routeSections` out of the three route frontmatters changed no rendered navigation. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check` and `npm run build` — the Zod schema is this collection's `check_schema` equivalent, so a missing or over-long field fails the build. Plus the built-output greps in `.claude/plans/issue-45.md` § Verification (23 `<dt>`, `id="net-interest"` once, zero `href="http`, zero `client:`, zero `<astro-island`, one `aria-labelledby="toc-heading"`, the base-prefixed nav link present on every page and the unbased form absent), and **six negative tests each proved to bite and reverted**: dangling `see_also`, `first_used` anchor that no longer exists, `first_used.route` outside the enum, `short` padded past 180, a Markdown body, and a term slug colliding with a `terms-<letter>` section id — plus the whole collection moved aside, which throws rather than rendering a stub. **Six pytest cases since #50**, in `pipeline/tests/test_pipeline.py` beside `test_check_sources_*`: `test_check_glossary_sources_is_clean_against_the_real_tree` (`failures == []` **and** `passed > 0`), `test_every_glossary_term_cites_only_registered_keys` (population is the 23 term files, not the register, so term twenty-four cannot ship unchecked), `test_check_glossary_sources_fails_on_an_unresolvable_key` (two terms mutated in one case, so the guard is generic), `test_check_glossary_sources_fails_on_a_missing_source`, `test_check_glossary_sources_fails_when_the_glossary_directory_is_empty` (the #37 rule: nothing to check is a failure) and `test_check_sources_rule_c_accepts_a_glossary_only_citation` (the widening, proved in both directions). Plus two negative experiments run by hand against `npm run build` and reverted: a blanked `source` and a bogus key, the second confirmed to emit no page at all | **NOT EXECUTED.** `/glossary` at 1440×900 and 390×844 (the A–Z rail marks the letter section containing the viewport midpoint; `#net-interest` lands clear of the sticky bar at 390px), and keyboard order: skip link, nav bar trigger, contents, content. Recorded as eight NOT EXECUTED rows M1–M8 in `docs/contracts/accessibility.md` § Manual pass results — the route postdates the 2026-08-26 browser pass and has not been walked. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | **No automated test asserts the five build-time throws in CI.** They are proved by the negative tests above, run by hand, and by nothing unattended — the pipeline suite reads `dist/`, so a build that fails never produces a page for it to check. A term's `source` **is** related to `SOURCES.md` since **#50** — `check_glossary_sources` resolves every key against `pipeline/curated/sources.yaml`, whose `registered_as` rule B pins to `SOURCES.md`, and it runs in the unattended pytest lane rather than only in a build. What that gate still cannot see is **whether the source a term cites is the right one**: `fiscal-year` citing CBO Historical Budget Data resolves exactly as cleanly as it would cite 31 U.S.C. §1102. Nothing asserts a term is actually defined or linked at its declared `first_used` point in the prose (#59), and nothing checks that the term list still covers the vocabulary the prose uses — a new specialist term added to a route is not detected as undefined |
| REF-3 | `pipeline/tests/test_contents_index.py`, eight cases over `dist/`: `test_contents_lists_every_route_the_rail_names` (route blocks == rail hrefs − `/contents`), `test_contents_lists_every_section_of_every_route`, `test_contents_lists_every_figure_in_route_document_order` (per route, the rendered `.figure-no` numbers are exactly 1..N ascending in built document order, and the index names each as `"<Route label>, Figure <n>."` with the same title — this is the guarantee the retired CSS counter used to give for free), `test_contents_figure_sections_match_the_built_placement`, `test_contents_lists_every_glossary_term`, `test_contents_links_are_base_path_joined`, `test_contents_carries_no_term_marker` and `test_contents_has_no_external_hyperlink`. **Both sides of the section, figure and term assertions read out of `dist/`** — the route's own built HTML supplies the sections and figure numbers it really renders — so none of them can be satisfied by `figures.ts` agreeing with itself. `test_curated_snapshots_expose_their_as_of` also moved here in spirit: it now asserts the manifest composes the two curated vintages and the government page composes none. The ~20 per-page accessibility cases pick `/contents` up by glob with no test-file edit, as they did `/glossary`. Since #51, `tests/test_prose.py` also reads this route's built page: `test_no_prose_string_contains_an_em_dash_or_a_double_hyphen` and `test_no_prose_string_shouts` over its `.prose`, `.standfirst`, `.finding` and `.figure-caveat` elements and its chart `aria-label`s, against the exact `KNOWN_DASH_DEBT` / `KNOWN_SHOUT_DEBT` baselines asserted with `==`, plus `test_the_dash_baseline_is_declining`, `test_prose_contract_cites_lines_that_resolve` and `test_prose_contract_has_a_numbered_rubric` over `contracts/prose.md` itself. | `npx astro check`, `npm run build`; the manifest's four build-time throws (duplicate key, section the route does not have, declared order contradicting section order, empty title or source) plus `/contents`'s own empty-collection and unresolvable-`first_used` throws; and the built-output greps in `.claude/plans/issue-49.md` § Verification (25 `.index-figure` == 25 `<Figure` call sites, 29 `.index-section`, 23 `.index-term` == 23 term files, one `href="/income-tax/government#net-interest"`, one `Government, Figure 13`, zero `href="http`, zero `.term`, zero `counter(figure)`). Two proofs run by hand and reverted: a throwaway section added to `routeSections['/households']` appeared on `/contents` with zero edits to `contents.astro`, and `dist/government/index.html`'s `<ol class="toc">` block is **byte-identical** to `main`'s build, the rail differing only by the new `<li>` | **NOT EXECUTED.** `/contents` at 1440×900 and 390×844 — the 390px row is the one that matters, since every line on this page is a derived string and the source lines are long, which is exactly how `/sources` overflows (#79); `.index-source` carries `overflow-wrap: anywhere` and the only `nowrap` is the short figure number. Plus a keyboard walk and ~8 followed links, one per route block, one term, one first-use anchor. Recorded as eight NOT EXECUTED rows M1–M8 in `docs/contracts/accessibility.md` § Manual pass results. **Prose: NOT EXECUTED**, and human-judged by construction — the seven-item checklist in `contracts/prose.md` § Checklist (read the route end to end; each finding against its chart and against its `aria-label`; terms before their definitions; causal claims against what the data supports; sentence rhythm out loud) needs a human reader, and no agent in this loop is one. The mechanical half was proved to bite on 2026-08-26: four negative tests executed and reverted — an em dash added to a `.prose` paragraph fails on the new fingerprint; a baselined em dash removed without shrinking the baseline fails on the missing entry; `THIS IS IMPORTANT` added to a `Figure` `note=` fails `test_no_prose_string_shouts`; a `path:line` past a file's end fails `test_prose_contract_cites_lines_that_resolve`. | **No automated test asserts the manifest's four throws in CI** — same gap as REF-2's five: the pytest lane reads `dist/`, so a build that fails produces no page to check. And nothing prevents the one residual numbering gap the manifest cannot express structurally — two figures inside a single section rendered in the opposite order to their declaration — from being *introduced*; `test_contents_lists_every_figure_in_route_document_order` catches it after the fact, on the next run |

## Cross-cutting

| ID | Pipeline (pytest) | Build-time | Manual | Gaps |
|----|--------------------|-----------|--------|------|
| DATA-1 | `test_every_published_output_has_a_schema` (the population is `src/data/*.json`, not the schemas that exist — no exemption set today, and if one is ever needed it is an explicit named frozenset, never a weakened assertion), `test_every_schema_names_a_published_output` (no orphan schema left behind by a rename), `test_check_schema_fails_when_an_output_has_no_schema` (**the guard-bites proof** — `validate.SCHEMA_DIR` is pointed at a copy of `pipeline/schemas/` with `budget.schema.json` removed and the call must yield exactly one failure naming `budget` and the path `schemas/budget.schema.json`; the same test asserts the inverse, zero failures against the intact directory, so it cannot pass by failing for an unrelated reason), plus the two sibling cases `test_check_schema_reports_malformed_json_rather_than_raising` and `test_check_schema_reports_an_invalid_schema_document_rather_than_raising`, and `test_every_schema_rejects_a_realistic_corruption` — 14 parametrized cases, one per output including the two pre-existing schemas, ids reading `budget-offsetting-receipts-turned-positive` and the like, sourced from `sorted(DATA.glob("*.json"))` rather than a hand-typed list so output number fifteen collects as `NO-CORRUPTION-CASE` and fails | `check_schema` in `pipeline/lib/validate.py`, run over every `*.json` in the staging directory by `build.py`; verified by hand on 2026-08-26 by moving `pipeline/schemas/economy.schema.json` aside — `uv run python build.py --tier monthly --dry-run` exited 1 with `BUILD FAILED`, `economy: no schema at schemas/economy.schema.json` and `Nothing was written.` | none — the gate is fully automated, and the guard-bites check is the automated analogue of #36's manual `client:only` flip | Schemas pin **shape and range only**: object keys, types, array `minItems`, numeric bounds and enums. Cross-field reconciliation (components summing to totals, the FY2003 series low, the non-partition rule, denominator checks) stays in `validate.py`'s per-output `check_*` functions and is not duplicated here. Bounds are set loose on purpose (`minItems` below today's length, `oecd.us_rank` unbounded above) so a legitimate source revision passes and only a real regression fails — see E5 in `.claude/plans/issue-37.md`. Nothing validates the schemas on the TypeScript side; `src/data/types.ts` is still hand-maintained and could drift from the schema without any test noticing |
| DATA-2 | `test_chained_and_core_cpi_start_at_their_own_first_year` (every row before FY2002 / FY1958 is `is None`, never `0` — 0 is falsy and 0 is the bug, so falsiness is not accepted; every row from the start year to the last projected row is non-null), `test_chained_and_core_cpi_are_index_levels_not_rates` (pins FY2002 `chained_cpiu == 105.132` and FY1958 `core_cpiu == 29.458` — a year-over-year rate would be single digits, so these values fix the series as index LEVELS and fail loudly if a future CBO vintage rebases either index), `test_phantom_zero_row_guard_rejects_a_duplicate_in_any_other_year` (the **ingest** guard-bites proof — `_drop_phantom_zero_row` is called directly with three cases in one test so it cannot pass by failing for an unrelated reason: the same duplicate shape at `(1986, "single")` raises `SourceUnavailable` with a message naming the duplicated floor and the phrase `refusing to guess`; the known `(1985, "single")` case drops the **phantom** and leaves the real `hi == 2390` row; a clean ladder is returned untouched), `test_check_bracket_history_rejects_a_duplicate_bracket_floor` (the **published-output** guard-bites proof — `validate.DATA_DIR` is pointed at a `tmp_path` copy of `bracket_history.json`: a single duplicated floor in 1985 single yields exactly one failure naming the year, the status and the floor; the same corruption in 1994 mfj proves the check is generic over every ladder, not 1985-special; the full phantom shape trips the 1985 fingerprint as well; and the inverse against the real data yields zero failures) | `check_bracket_history` in `pipeline/lib/validate.py` — strictly increasing `lo` in every year/status ladder plus the 1985-single fingerprint (16 brackets, exactly one 0% bracket ending at $2,390, no open-ended zero-rate bracket, top 50% above $85,130), run on every build including `--tier monthly` because `build.py` copies `src/data` into staging. `check_schema` against `pipeline/schemas/economy.schema.json`, which now requires `chained_cpiu` and `core_cpiu` as `["number","null"]` with `not: {const: 0}`, so a zero-fill fails the build. `check_prose_figures` reconciles 118 registered figures including the new `debt_holders.data.split.1.share_pct`. Gate: `uv run python build.py --tier monthly --dry-run` | none — every check here is automated | **Nothing on the site consumes `chained_cpiu` or `core_cpiu` yet.** #38's scope is "emit"; §4 still charts `cpi` and `core_pce` only, so the two new series have pipeline invariants as their sole coverage and no component or e2e test touches them. The duplicate-floor check reads the published output, so it cannot catch a corruption introduced between `emit.write` and a hand-edit of `src/data`; and the 1985 fingerprint is a spot check on one ladder, not a full re-derivation of all 113 years against the primary source |
| DATA-3 | `test_source_register_covers_every_published_output` (the population is `src/data/*.json`, not the register entries that happen to exist, and the converse is asserted in the same test so an entry orphaned by a rename cannot keep the count whole), `test_check_sources_is_clean_against_the_real_tree` (**the inverse, asserted first** — zero failures against the real `SOURCES.md` and the real `src/data/`, so nothing below can pass by failing for an unrelated reason), `test_check_sources_fails_when_a_cited_source_is_not_registered` (**the guard-bites proof for rule B**, the defect #39 was filed for — `validate.SOURCES_DOC` is pointed at a `tmp_path` copy of `SOURCES.md` with a source's lead-in replaced, and the call must yield exactly one failure naming that register key and `SOURCES.md`; run for `irs_soi_table_5` and `census_stc` in one test body so the guard is proven generic over the register rather than special-cased to whichever source happened to be missing), `test_check_sources_fails_when_an_output_cites_an_unregistered_source` (**the guard-bites proof for rule D**, the case B cannot see — `validate.DATA_DIR` is pointed at a `tmp_path` copy of `src/data/` with `"; Some Unregistered Source"` appended to `states_tax_mix.json`'s `_meta.source`; exactly one failure, naming the output and the unaccounted text), `test_check_sources_tolerates_a_vintage_refresh` (**the loose-on-purpose proof** — `budget.json`'s `Feb 2026` rewritten to `February 2027` yields zero failures, so a future author cannot tighten `_normalize_source` into an exact match and turn every ordinary upstream republication red, which is how the check would end up disabled), `test_normalize_source_strips_dates_but_keeps_identifying_numbers` (**the counterweight to that proof** — vintage tolerance must not be bought by erasing every digit, so this asserts each date form normalizes away *and* that `Table 5`/`Table 23`, `MEHOINUSA672N` and `PL 115-97` survive and stay distinct), `test_check_sources_fails_when_a_cited_document_number_changes` (**the guard-bites proof for the third direction** — `cbo_effective_rates.json`'s `Supplemental Data Table 9` rewritten to `Table 4` leaves the wording and the registered source untouched yet must still yield exactly one failure, which only a normalizer that keeps identifying digits can see) | `check_sources` in `pipeline/lib/validate.py`, called from `run()` **unconditionally** alongside `check_meta` and `check_schema` — never behind an `if "x" in outputs:` gate, because a check skipped because its output was not in the tier reads exactly like a check that passed (#37). Gate: `uv run python build.py --tier monthly --dry-run`; verified by hand on 2026-08-26 by replacing the `**IRS Statistics of Income, SOI Data Book Table 5...**` lead-in in `SOURCES.md` — the run exited 1 with `irs_soi_table_5: cited by this site but NOT registered in SOURCES.md` and `Nothing was written.` | none — the gate is fully automated, and both guard-bites directions have automated proofs | The register is matched **into** `SOURCES.md`, never scraped **out of** it, so nothing asserts that a registration is a *good* description of its source — only that the string is present. `_normalize_source` strips **dates only** — years, month names, day-of-month and a Revenue Procedure's `-NN` serial — so two sources differing only in an identifying number (`Table 5` vs `Table 23`) no longer collapse onto each other; the residual risk is inverted, in that a genuine vintage carried in some digit form the normalizer does not know about would turn the build red on an ordinary refresh. Rule D compares a stored shape, so it catches a source **added** to a source line but not a source silently **swapped** for one already in the register under the same citation string. And nothing checks the reverse direction of the register itself: a `SOURCES.md` entry cited by no output (CBO's Estimates of Automatic Stabilizers today) is simply absent from `sources.yaml` and is not reported |
| DATA-4 | Four offline cache-contract tests in `pipeline/tests/test_pipeline.py`, all of which `monkeypatch.setattr(lib.fetch, "CACHE_DIR", tmp_path)` — which works because `_cache_path` reads the module global at call time — prime an entry by hand in the exact on-disk shape, and aim the call at an unroutable `.invalid` host, so a cache hit returns and a cache miss can only raise: that is what makes them prove *no request happened* rather than merely that the right value came back. `test_warm_text_cache_is_served_without_a_request` (the `{url, text, retrieved_at}` shape, `from_cache is True`), `test_binary_cache_round_trips_through_base64` (the `{url, b64, retrieved_at}` shape, bytes back byte-for-byte, including non-UTF-8 payloads), `test_post_json_cache_key_includes_the_payload` (**both directions in one body** — payload A is served from a hand-primed entry, proving the `url + "|" + json.dumps(payload, sort_keys=True)` derivation is intact, and payload B must raise `SourceUnavailable` rather than quietly return A's body, which is the USASpending fiscal-year collision), and `test_a_binary_cache_entry_does_not_crash_a_text_fetch` (**the one intentional behaviour delta of #40, pinned** — it fails with `KeyError: 'text'` on the pre-#40 module and passes after, so the guard cannot be silently dropped). Plus the pre-existing `test_unreachable_source_raises_rather_than_returning_empty` and `test_truncated_body_is_treated_as_failure`, which exercise the same unified core through `fetch` | n/a — the pipeline is not part of the Astro build | The warm-cache probe, run on #40: `STAMP=$(date +%s); sleep 1; cd pipeline && uv run python build.py --tier monthly --dry-run >/dev/null && find .cache -newermt "@$STAMP" | wc -l` must print `0`. Zero cache files rewritten is zero HTTP requests, which is the only way to observe that a refactor did not silently invalidate the 67 warm entries and turn a warm run into a full refetch that still reports success | The two older failure tests hit the real network and would **also pass during an outage**, since an outage raises the same `SourceUnavailable` they assert — a green run of those two is weaker evidence than it looks. No test covers the non-200 or short-body branches offline (both need a stub transport, which would be the first test double in this suite). `post_json`'s decode-before-cache ordering is asserted only by reading the code, not by a test. `pipeline/lib/sources.py:145` builds its own `httpx.Client` for a `HEAD` probe and is outside this core entirely — parked, not covered |
| DATA-5 | `test_check_meta_titles_is_clean_against_the_real_tree` (**the inverse, asserted first** — zero failures over all 14 published outputs, so nothing below can pass by failing for an unrelated reason), `test_no_curated_title_types_a_literal_year` and `test_curated_titles_render_to_their_published_form` (rule A at the source, plus the proof that templating reworded only the two outputs #41 aimed at and left the other twelve byte-identical), `test_check_meta_titles_bites_on_the_defect_it_was_filed_for` (**the guard-bites proof** — the exact pre-#41 curated budget title is restored through `curated.meta_for` and the call must yield exactly one failure naming `budget` and `curated/notes.yaml`; this is the case a coverage-only comparison cannot see, because FY1995-FY2025 *is* a declared window, `control_start`/`control_end`, just not the one the sentence attached it to), `test_check_meta_titles_fails_a_range_coverage_does_not_declare` (rule B, `validate.DATA_DIR` pointed at a `tmp_path` copy with budget retitled FY1900-FY2025), `test_check_meta_titles_fails_a_range_with_no_coverage_at_all` (`coverage: null` is **unknown, not pass** — a range added to `income_tax_by_group`'s title yields one failure naming `TITLE_RANGE_EXEMPT`), `test_the_only_exemption_carries_a_written_reason_and_is_not_a_silent_skip` (both halves: the entry names why, and with the dict emptied `cbo_effective_rates` goes red under both rules — so the exemption is what holds it, not a regex that cannot see the range), `test_open_ended_titles_assert_a_start_and_not_an_end` (`economy.coverage.end` is the 2036 projection horizon; "FY1950 onward" asserts the start only, and FY1949 fails), `test_iso_date_coverage_bounds_compare_as_years` (`party_splits` carries ISO dates, not integers), `test_nested_per_series_coverage_blocks_are_windows_a_title_may_name`, and `test_expand_title_raises_rather_than_publishing_a_placeholder` (a `{start}` in a published JSON file is worse than the stale range it replaced, so emit raises on both no-coverage and unknown-key, and passes a placeholder-free title through verbatim) | `check_meta_titles` in `pipeline/lib/validate.py`, called from `run()` **unconditionally** beside `check_meta`, `check_schema` and `check_sources` — never behind an `if "x" in outputs:` gate (#37); gate: `uv run python build.py --tier monthly --dry-run`, verified on 2026-08-26 by restoring the literal budget title, which exited non-zero before the fix and clean after | none — the guard is fully automated, and the guard-bites direction has an automated proof | Rule A reads `curated/notes.yaml`, so it cannot see a title typed straight into `src/data/*.json` by hand; rule B is what covers that case, and rule B is deliberately loose — it accepts **any** declared window, so a templated clause pointing at the wrong one of two windows would still pass. The regex knows two title shapes, a `YYYY-YYYY` range and `YYYY onward`; a title inventing a third ("since 1962", "through the 1990s") is not checked at all and is not detected as unchecked. Nothing asserts a title is a *good* description of its data, only that its years agree with coverage |
| DATA-6 | `test_brief_file_list_paths_exist` (every first token in the `## Files in this folder` fence resolves under the repository root; the parser **raises rather than returning empty** if the section or its fence moves, since a check that finds nothing to check reads exactly like one that passed — #37's rule applied to a doc), `test_brief_file_list_names_the_brief_itself_and_every_root_json` (the two claims existence alone cannot catch: the first line named `README.md` as "this brief", and `README.md` **does** exist, so a resolving path was still the wrong file; and the eight curated inputs are asserted present with no `data/` or `content/` prefix, globbed from the root rather than hand-listed so a ninth input cannot be quietly omitted) | none — `BRIEF.md` is not part of the Astro build | Guard-bites verified by hand on 2026-08-26: `oecd-tax.json` moved aside, `uv run pytest -k brief` went red naming the missing path, green again on restore | **Existence-only, deliberately.** Nothing asserts the list is *exhaustive*: a new root file is not required to appear, because an exhaustiveness check would fight every addition to the repository and end up disabled — the failure mode that let this block drift for the length of the build. Nothing checks the prose description beside each path either, so a file whose contents change purpose keeps its old one-line gloss. `test_brief_file_list_names_the_brief_itself_and_every_root_json` closes the exhaustiveness gap for the root JSON inputs only, which is the half that actually drifted |
| A11Y-1 | `pipeline/tests/test_accessibility.py` — a generic static conformance suite over every built page and every island component | `npx astro check`, `npm run build` | The itemised "Shared" checklist in `docs/contracts/accessibility.md` (Tab/Shift-Tab traversal, screen-reader pass, focus-trap check, 390px legibility, greyscale render, Safari focus-ring visibility, measured pixel contrast) — **executed 2026-08-24 / 2026-08-26**, Chrome 151 and WebKit 26.5, results one row per check per route in `docs/contracts/accessibility.md` § Manual pass results, now eight checks (M1–M8) at 32 rows (8×4 routes). Seven of the eight ran; the screen-reader pass (M2) did not, and the Safari focus-ring item (M7) ran only in headless WebKit, not Safari.app — both carried by #80 | No browser-driven test of any kind exists in this repo, so the checks above are a dated manual observation rather than a regression guard — they were run by hand and nothing re-runs them. #67 adds the CI check that would. #80 holds the two items no agent can execute |
| A11Y-2 | none | none | The itemised "Per-consumer" checklist in `docs/contracts/accessibility.md` (cross-route keyboard sweep, screen-reader passes per route, keyboard-model checks per interactive primitive) — cross-route keyboard sweep **executed 2026-08-24**, Chrome 151, tab stops counted per route (`/` 408, `/government/` 471, `/households/` 356), FAILs #69, #71, #72; keyboard-model checks **executed** against the three control shapes that actually render — `role="radio"` groups, filter dropdown buttons and `<details>`/`<summary>` disclosures — the anticipated `Select`/`Slider`/`Dialog`/`Tabs`/`Tooltip` consumers never landed and the site renders zero `<input>` and zero `<select>`; screen-reader passes **NOT EXECUTED — human plus an assistive technology required, #80** | Same automation gap as A11Y-1. The row sits at `In progress` rather than `Shipped` because #80 is open; it is the named blocker, not an open-ended one |
| A11Y-3 | `pipeline/tests/test_accessibility.py` — seven new checks: `test_nav_bar_mirrors_every_route_and_section` (per page, set equality of the panel's route hrefs and contents fragments against the rail's, so the two lists cannot drift — 6 routes everywhere since `/glossary` joined the route array, and 12/7/6/13/0/0 sections by route for `/government`, `/households`, `/economy`, `/glossary`, `/` and `/sources`), `test_nav_bar_does_not_precede_the_skip_link` (document-order index of `.skip-link` below that of the bar's `<summary>`), `test_no_page_repeats_an_id` (generic; the duplicated contents list made a second `id="toc-heading"` the live hazard, and `id_map` silently keeps the last of a duplicate so every existing `aria-labelledby` check would still have passed), `test_nav_bar_panel_scrolls_internally`, `test_sticky_nav_bar_offsets_its_anchor_targets`, `test_nav_bar_open_close_is_not_animated` and `test_nav_bar_tap_targets_clear_44px` — the last four read a new `narrow_media_block()` helper, which returns the raw body of `@media (max-width: 62rem)` and **raises rather than returning `""`** if that block moves, because `iter_css_rules` flattens every @-rule and discards its condition and a helper that finds nothing to check reads exactly like one whose checks passed. Plus `".navbar a"` appended to `_TEXT_SELECTORS`, which holds the bar's link colour to the same 4.5:1 floor as `.rail a` via `test_no_text_selector_paints_with_a_low_contrast_token` (it fails hard if the selector is absent from `global.css`, so it cannot pass by finding nothing). Guard-bites verified by hand on 2026-08-26: stripping `overflow-y: auto`, dropping `#main`'s `scroll-margin-top`, adding a `transition` to `.navbar-chevron`, lowering the tap-target floor to 2rem, renaming `navbar-toc-heading` back to `toc-heading` and slicing one route out of the panel each turned exactly the intended test red | `npx astro check` (0 errors), `npm run build`, `npm run test:unit` (17/17), plus the built-output greps in `.claude/plans/issue-42.md` § Verification — one `class="navbar"` per page, 24/14/12 `data-section` on Government/Households/Economy, zero `navbar-toc-heading` on `/` and `/sources`, exactly one `id="toc-heading"` on Government, and the **desktop-unchanged proof**: once the `.navbar` block, the inline `<script>` and the `data-section` attributes are removed and the stylesheet content-hash normalised, `dist/government/index.html` and `dist/index.html` are byte-identical to their pre-change builds | **EXECUTED 2026-08-26**, Chromium via Playwright, `/`, `/economy`, `/households`, `/government`, `/sources`. At 390×844: the bar is `position: fixed` with `top === 0` and `height === 52` at `scrollY = 0` and at the document bottom on all five routes; Government's 12-entry panel has `scrollHeight 804 > clientHeight 776` while `document.body.scrollHeight` is identical (25,329) closed, open and closed again — and the same in **landscape 844×390** (804 > 322, body 22,920 throughout), which is the worst case; with all 13 tables open the page grows 25,329→37,018px and the bar is still pinned at `top 0` at `scrollY 36,174` and still opens. Tab stop 1 `.skip-link`, stop 2 the `<summary>` (accessible name "Government — routes and contents"), stop 3 inside `main`; Enter opens and focus lands on `#navbar-panel`; Escape closes and focus returns to the `<summary>`; Tab from the last panel link lands inside `main`, not wrapped; an in-panel contents link closes the panel and `#net-interest` lands at `top 64 >= 52`; an outside click closes it; the `/government/` filter dropdowns still own Escape and still restore their own focus. Every `a` and `summary` inside `.navbar` measures ≥ 44×44 on all five routes (6/12/13/18/6 elements). With `javaScriptEnabled: false`: 17 links in the closed DOM, opens by click and by Enter, closes by Enter, and Escape correctly does nothing. At 1440×900: `.navbar` computes `display: none`, `.rail` computes `position: sticky`, and a full Tab traversal of `/government/` counted 438 stops with **zero** inside `.navbar`. **Re-measured for #46 on 2026-08-26** as the site's back-to-contents affordance, from `#limits` — the deepest section — rather than from the top: at 390×844 the `<summary>` is on screen (`top 3.5`, `bottom 47.5` in an 844px viewport) and clears 44×44 on all three routes with a contents list, measuring **83.8 × 44** `/economy`, **107.0 × 44** `/households`, **109.5 × 44** `/government` (WebKit 26.5; Chromium within 1px). At 1440×900, from the same position, `.navbar` computes `display: none`, `.rail` computes `position: sticky`, and the rail's whole **636px** runs `top 0` → `bottom 636` inside a 900px viewport with the twelfth of twelve contents links on screen — the measurement behind #46's folding call | No browser-driven regression guard exists for any of the manual column — the same gap as A11Y-1 and A11Y-2; #67 is the issue that would add it, and nothing re-runs the Playwright sequence above. Whether the native disclosure **announces** its expanded/collapsed state correctly is a screen-reader question and is **NOT EXECUTED** — #80. Section-level `aria-current` in the panel is #44 and scroll restoration is #46, both deliberately out |
| A11Y-4 | `pipeline/tests/test_accessibility.py` — five new checks: `test_no_built_page_ships_a_section_level_aria_current` (per page; zero `aria-current="true"` nodes in the server-rendered HTML **and** `aria-current="page"` at exactly 2, so a build that dropped the route markers entirely could not turn the zero-check green), `test_every_contents_anchor_is_addressable_by_the_spy` (per page; every `.toc` and `.navbar-toc` anchor carries a `data-section` and the two lists' values equal `main section[id]` in document order — a section added to a page but not to its `sections` array is otherwise silently unmarkable; `/` and `/sources`, which pass no `sections` prop, return early, matching the IIFE's own early return), `test_contents_lists_are_not_live_regions` (per page; no `.toc`/`.navbar-toc`, no ancestor and no descendant carries `aria-live`, `aria-atomic` or a live-region `role` — **this is the proof of the silence criterion**, not an assumption), `test_section_state_selector_is_scoped_and_not_bare` (over `global.css` via `iter_css_rules`: at least one `[aria-current='true']` rule scoped to the contents list, and **no** bare `[aria-current]` selector anywhere — it raises rather than passing silently if it finds no scoped rule at all) and `test_the_section_spy_introduces_no_scripted_scrolling` (over `BaseLayout.astro`: exactly one `<script is:inline>` block, `IntersectionObserver` present in it so the check cannot pass by finding no script, and none of `scrollIntoView`, `scrollTo(`, `scrollBy(`, `behavior:`). Guard-bites verified by hand on 2026-08-26: hard-coding `aria-current` onto the rail's first anchor, dropping `data-section` from the panel list, adding `aria-live="polite"` to `.toc`, widening the CSS selector to a bare `[aria-current]` and adding a `scrollIntoView` call to the IIFE each turned exactly the intended test red | `npx astro check` (0 errors), `npm run build`, `npm run test:unit` (17/17, unchanged — no TS module is touched), `uv run pytest` (298 passed, 4 skipped), plus the built-output greps in `.claude/plans/issue-44.md` § Verification: `aria-current="true"` **0** and `aria-current="page"` **2** on all five built pages, `IntersectionObserver` present in `dist/government/index.html` (`is:inline` scripts are not bundled, so it lands in the page), zero `\.rail [a-z]*\[aria-current\]` in the built CSS, and zero `scrollIntoView|scrollTo\(|behavior:` in `dist/government/index.html` | **EXECUTED 2026-08-26**, Chromium 151.0.7922.174 via Playwright against `astro preview`, `/`, `/economy`, `/households`, `/government`, `/sources`, at 1440×900 and 390×844; 0 console errors and 0 warnings throughout. Top of page marks section 1 on all three routes with a contents list (`#forty-trillion` / `#one-picture` / `#what-a-household-earns`); the document bottom marks `#limits` on all three at both viewports. **Monotonicity:** 200px-step sweeps — `/government` 112 samples at 1440×900 and 128 at 390×844, **0 decreases**, all 12 sections visited; `/economy` 49 samples, 0 decreases, 6 of 6; `/households` 53 samples with the exact document bottom appended, 0 decreases, 7 of 7 (its `limits` is 1058px against an 844px viewport, so the midpoint never enters it before the bottom rule fires — E7 observed rather than argued). `#the-laws` (5.38 × viewport) held the mark across all 24 samples inside its bounds and `#by-state` (4.99 ×) across all 22. At **every** sampled position the DOM carries 2 `aria-current="true"`, one per list, with the two hrefs equal — including with the panel held open while the page scrolls behind it (0 → 19,000px). All 12 panel links clicked in turn at 390×844: the mark equals the clicked link every time, the panel closes each time, and the target lands at top 64 clearing the 52px bar. `/` and `/sources` show **0** marks with JavaScript **on** and no console error — the early return. With `javaScriptEnabled: false`: **0** `aria-current="true"` and **2** `aria-current="page"` on all five routes, paired against the same context with scripting on (2 at load, no scrolling), which is what makes the difference attributable to the script rather than the server; #36's guard held in that same run, 14 of 14 `figure.figure svg.chart` server-rendered on `/government` with scripting off. The rail's contents `<ol>` measures 208 × 314.34 before and after a mark change — no layout shift. **Desktop-unchanged proof:** with the stylesheet content-hash normalised, `dist/government/index.html` and `dist/index.html` each differ from their pre-change build by **92 added lines and zero removed lines**, all of them the `sectionSpy()` block | No browser-driven regression guard exists for the manual column — the same gap as A11Y-1, A11Y-2 and A11Y-3. The 200px-step monotonicity sweep is the **only** anti-oscillation evidence and nothing re-runs it; #67 is the issue that would. Whether a screen reader **reports** the marked section on demand is not observable without an assistive technology and is **NOT EXECUTED — #80** (checklist item 13); only the silence half is provable statically. Scroll restoration across a route change is #46 and is deliberately out |
| A11Y-5 | `pipeline/tests/test_accessibility.py` — five new checks, each asserting an **absence** and each paired with a positive assertion so it cannot pass by reading nothing: `test_scroll_restoration_is_left_to_the_browser` (the string `scrollRestoration` appears in none of the `.astro`/`.ts`/`.tsx`/`.css`/`.json`/`.md` files under `src/`, and `BaseLayout.astro` is asserted to be among the files scanned; over `src/` rather than `dist/` because `is:inline` scripts are not bundled and a future island's code would be, so only the source covers both), `test_no_stylesheet_requests_smooth_scrolling` (over `global.css` via `iter_css_rules`: every `scroll-behavior` declaration reads `auto`, **and** at least one exists — the `prefers-reduced-motion` block's `auto !important`), `test_scroll_anchoring_is_not_disabled` (no rule declares `overflow-anchor` at all, paired with `.navbar-panel`'s real `overscroll-behavior: contain`; the absence is load-bearing and nobody would think to look for it), `test_the_layout_registers_no_bfcache_disqualifying_listener` (the substring `unload` appears nowhere in the layout's inline script — one check covering `unload`, `beforeunload` and `onunload` — while `addEventListener` does appear) and `test_no_built_page_scripts_a_scroll` (per built page: none of `scrollIntoView`, `scrollTo(`, `scrollBy(`, `behavior:` in the served HTML, with `IntersectionObserver` present so the zero is meaningful). Plus a new `layout_inline_script()` helper beside `narrow_media_block()`, which **raises rather than returning `""`** if the single `<script is:inline>` block moves or is split; #44's `test_the_section_spy_introduces_no_scripted_scrolling` is refactored onto it in the same commit rather than left with a second parser, and `_SCROLL_API_RE`'s `behavior:` clause gained a `(?<![\w-])` lookbehind so it matches the scroll options bag and not the CSS properties `overscroll-behavior`/`scroll-behavior`. **The `src/`↔`dist/` overlap between `test_the_section_spy_introduces_no_scripted_scrolling` and `test_no_built_page_scripts_a_scroll` is deliberate — neither is redundant; do not delete one.** Guard-bites verified by hand on 2026-08-26, each turning **exactly** the intended test red and no other: assigning `history.scrollRestoration = 'manual'` in the layout script, appending `html { scroll-behavior: smooth }`, appending `main { overflow-anchor: none }`, adding a `beforeunload` listener to the layout script, and injecting a `scrollIntoView()` call into `dist/government/index.html` (injected into the built page, not the source, so it isolates the `dist/` check from #44's `src/` one) | `npx astro check` (0 errors), `npm run build`, `npm run test:unit` (17/17, unchanged — no TS module is touched), `uv run pytest` (327 passed, 5 skipped), plus the greps in `.claude/plans/issue-46.md` § Verification: `scrollRestoration`, `sessionStorage\|localStorage`, `overflow-anchor` and `'unload'\|'beforeunload'` **0** each over `src/`; `scrollIntoView\|scrollTo(\|scrollBy(\|behavior:` **0** on all six built pages with `IntersectionObserver` ≥ 1 on each; `<details class="navbar-disclosure"` **1** on `/economy`, `/households`, `/government` and `<button[^>]*navbar-disclosure` **0**; `<body>[^<]*<a class="skip-link"` **1** | **EXECUTED 2026-08-26**, Playwright against `astro preview`, **Chromium 151.0.7922.34 and WebKit 26.5**, at 390×844 and 1440×900. **bfcache was not in play for any measurement** — a `window.__marker` set before leaving survived no back navigation — so every number is the full-reload worst case, and `history.scrollRestoration` read `'auto'` in every context. *Sequence 1, plain Back* (scroll to a section, go to `/sources/`, `history.back()`, settle 2.5s, compare `getBoundingClientRect().top`): Chromium **+0.3 / −0.7 / +0.3px** at 390×844 on `/economy#prices-rates`, `/households#who-pays`, `/government#by-state`, and **0.0px** on all three at 1440×900, holding through Forward-then-Back-again; `#by-state` lands at `top 64.58` = `calc(var(--navbar-h) + 0.75rem)`, so #42's `scroll-margin-top` is honoured by the restore. WebKit **−114.7 / −238.7 / −237.7px** at 390×844 and **0.0 / −124.0 / −4.0px** at 1440×900. *Sequence 2, hash URL* (arrive at `/government/#by-state`, read on to `#the-laws`, leave, return): **both engines restore the position, not the anchor** — `#the-laws` back at `top 63.8`/`63.6`/`0.27`, **0.0px drift** in all four combinations, with `#by-state` 9,237–9,287px below the viewport top and the URL still carrying the fragment. *Sequence 3, opened tables* (all 13 `main details` open, scroll to `#the-laws`, leave, return): `open` is restored by neither engine, the document comes back **11,854–11,970px shorter** (37,226 → 25,256px, Chromium 390), and Chromium's scroll anchoring absorbs the whole shrink — `#the-laws` drift **−0.1px** at 390×844 and **−0.4px** at 1440×900 — while WebKit lands **−6,826px / −6,592px** short. `aria-current="true"` count is **2** (one per list) in every case and names the **restored** section, not the pre-restore one: `the-laws` under Chromium and `where-money-comes-from` under WebKit, each matching where its own engine actually put the reader. *Scripting off:* sequence 1 restores `scrollY 6000` → `scrollY 6000`, **0px drift**, both engines, both viewports; the disclosure is still a native `<details>` with 18 links and 0 `<button>`s and still opens on click at 390×844 | Three gaps stay open, none of them closed by this row. (1) **WebKit has no scroll anchoring.** Plain Back drifts up to 238.7px — 28% of an 844px viewport, inside the section the reader left, so #46's decision procedure ships it as-is — but the collapsed-`<details>` case lands ~6,800px short, about eight screens, and iOS Safari is the narrow-viewport audience. Measured and recorded in `docs/contracts/accessibility.md` § Scroll restoration and the back button (#46); **not repaired**, because the repair is the manual implementation #46's criterion 1 exists to keep out. (2) No browser-driven regression guard exists — the five tests here are **static only**, and every number in the manual column is a dated hand observation that nothing re-runs; #67 is the issue that would add one, and it matters more here than elsewhere because #46's entire deliverable *is* a guard. (3) Whether a screen reader **reports** the restored position on return is not observable without an assistive technology — **NOT EXECUTED, #80** |
| A11Y-6 | `pipeline/tests/test_accessibility.py` — five new checks, each paired with a positive assertion so it cannot pass by reading nothing: `test_every_term_marker_is_a_real_link` (per page; each `.term` holds exactly one `.term-trigger`, it is an `<a>` and never a `<button>`, its `href` is exactly `/income-tax/glossary#<data-term>` — #70's unbased-link failure mode — and it carries `aria-describedby="def-<slug>"`), `test_every_term_marker_names_a_real_glossary_entry` (per page; every `data-term` is a filename under `src/content/glossary/`), `test_no_page_marks_a_term_twice` (per page; `data-term` values unique — the machine-checkable half of "only the first use is marked", and also what stops a repeated `def-<slug>` id), `test_every_first_used_route_carries_its_term_marker` (ties #45's `first_used` data to #47's markup in **both** directions: every term's `first_used.route` page carries its marker unless it is in the explicit `UNMARKED_AT_FIRST_USE` set, every id in that set is still a real term, and no term in it is marked on the route it names — so a rename fails, a new unmarked term fails, and a later prose edit that makes one markable forces the exception to be deleted rather than left stale), and `test_term_popovers_are_not_animated` (no `transition`, `animation` or `@keyframes` under any `.term*` selector in `global.css`, plus, per page, exactly one `.term-pop` **inside** each wrapper — the DOM-descendant shape that satisfies 1.4.13 *hoverable*, which a portal would take away — `hidden` in the served HTML, `id="def-<slug>"`, and a `.term-short` whose text is the term's `short` verbatim). Verified to fail on removal: deleting one `<Term>` from `/economy` turned `test_every_first_used_route_carries_its_term_marker` red. Full suite **327 → 352 passed, 5 skipped**, including the ~31 auto-collected per-page cases on all **six** built pages | `npx astro check` (0 errors, 0 warnings), `npm run build` (6 pages), `npm run test:unit` (unchanged, still green), `uv run python build.py --tier monthly --dry-run` (0 drift, 0 validation failures). Built-output greps: `class="term-trigger" href="/income-tax/glossary#` **6 / 8 / 12** on `/economy`, `/households`, `/government`; `href="/glossary#` **0** across all six pages; `<button` **unchanged** at 57 / 2 / 0 / 0 / 0 / 0 against the pre-change baseline; zero `aria-live`, `aria-atomic` or `role="status"` in the added markup or script. A bad `<Term id>` is a named build-time throw, not a rendered dead link | **EXECUTED 2026-08-26**, Chromium **151.0.7922.174** (Playwright MCP) against `astro preview` at 390×844 and 1280×900, scripting on and off. Rows `M9`–`M12` in `docs/contracts/accessibility.md`, and the expected/actual keyboard model as the fourth bullet inside its checklist item 8. `M9` all 26 popovers 350px wide and inside the viewport, `scrollWidth` 390, one open at a time, the two-line trigger case included. `M10` Tab opens, Tab reaches the popover's link with it still open, Tab again closes and continues, Shift-Tab returns with it open, Escape closes with focus on the trigger and `scrollY` unchanged; the `/government/` filter dropdowns and the nav panel still own Escape. `M11` one touch tap opens with `location.href` unchanged, a real mouse move onto the popover does not dismiss, 3s with no input does not dismiss, modifier and middle clicks still reach `/glossary`. `M12` scripting off: 26 plain `<a>`, 0 `aria-expanded`, 0 rendered popovers, every `short` still in the DOM, the link followable. Console clean throughout | One gap, and it is the same one #80 carries for every other row: whether a screen reader **announces** the popover as the trigger's description. VoiceOver + Safari and NVDA + Firefox are not drivable from this environment — **NOT EXECUTED**, human required, **#80**. The static half (`aria-describedby` resolves to an in-DOM element inside the same wrapper carrying the `short` verbatim, no portal, no live region) is asserted. Separately, whether a marker sits on the genuinely *first* prose occurrence is a reading check and is deliberately not a test; six terms carry no marker on their `first_used` route because that route's prose never names them, listed with reasons in `contracts/interfaces/glossary.md` |

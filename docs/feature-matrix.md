# Feature matrix

Single view of what exists per route and how far along it is. One table per route, ordered as a
reader encounters the sections. Status vocabulary is fixed: `Planned`, `In progress`, `Shipped`,
`Deprecated`. A row moves to `Shipped` only when its issue's full definition of done passes, not
when the code merges.

## Introduction route (`src/pages/index.astro`)

| ID | Capability | UI | Data | Status | Issue | Contract |
|----|-----------|----|------|--------|-------|----------|
| INTRO-1 | Front door — what the site is and what it refuses to do; five cards naming every destination, the three routes carrying `Route N of 3` and a section count derived from `routeSections`; how to read a figure (the ladder, the caption's Units/Note/Source, view-as-table, the unit toggle); and the data-integrity promise stated as four build-time failures. Static HTML, no island, no chart, no term marker. Since #58 the page meets Criterion 3: the two em dashes in §§1-2 became a comma pair and a colon, §3's appositive dash became a full stop and a new sentence, and the two sentences that ran past the 45-word cap were split where their inventories begin | done | n/a | Shipped | #43, #48, #58 | contracts/accessibility.md, contracts/prose.md |

## Economy route (`src/pages/economy/index.astro`)

| ID | Capability | UI | Data | Status | Issue | Contract |
|----|-----------|----|------|--------|-------|----------|
| ECO-1 | One picture — real GDP FY1950-FY2036 on a log scale, actual/projection split. **#59** marks §1's standfirst at the reader's true first meeting with three terms: `GDP` and `FY2025` join the `real` and `chained dollars` markers already there, and `fiscal-year`'s marker moves up from the derivation paragraph below, where it had been sitting 633 characters after the reader first met "FY2025". `CBO` gains a marker in that paragraph. | done | done | Shipped | #12, #59 | contracts/interfaces/economy-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| ECO-2 | Growth and its shadow — output per hour vs. real median household income, indexed 1984=100. The 1984 = 100 base is stated once, in the standfirst that sets the chart up; the finding gives the two 2024 index values and nothing else. Since #58 the standfirst no longer serves "Realmedian": the `<Term>` had wrapped onto its own source line and Astro dropped the space in front of it. Criterion 3 | done | done | Shipped | #12, #53, #58 | contracts/interfaces/economy-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| ECO-3 | Who works — unemployment vs. the noncyclical rate, and labour force participation | done | done | Shipped | #12 | contracts/interfaces/economy-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| ECO-4 | Inflation and rates spiked together, then spent a decade near zero — CPI-U and core PCE inflation (derived YoY), fed funds, 3-month bill, 10-year note. The heading states what the section settles rather than naming its two variables, and the standfirst says why the two panels share a section instead of restating the finding, or the rate it quotes. The finding is one claim, that the rate peak arrived a fiscal year after the price peak; the current levels and the near-zero decade are in the closing prose where a reader meets them after the chart. Since #58 the closing prose meets Criterion 3's 45-word cap — the 62-word derivation sentence is three, split at each derivation rather than at a comma. **#59** marks `CPI-U` in the finding and `core PCE` in the prose below it, the two acronyms this section is built on. The finding is deliberately the same sentence as the chart's accessible name and stays so: `_deep_text` skips `.term-pop`, so the served text is byte-identical. | done | done | Shipped | #13, #34, #52, #53, #58, #59 | contracts/interfaces/economy-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| ECO-5 | The wage share fell while the profit share rose — wages and salaries against corporate profits, both % of GDP. The heading states the movement the two lines show; the standfirst gives the shared denominator and the question, not the finding's figures. Since #58 the pandemic-denominator paragraph meets Criterion 3's 45-word cap, split where the explanation begins rather than at the colon | done | done | Shipped | #13, #52, #58 | contracts/interfaces/economy-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| ECO-6 | Limits — what this route cannot tell you, hand-off to /households. Since #58 the national-aggregates limit takes parentheses rather than an em dash pair, because the aside is itself a list (Ruling 1, row 1). Criterion 3 | done | done | Shipped | #13, #58 | contracts/interfaces/economy-data.md, contracts/prose.md |

## Government route (`src/pages/government/index.astro`)

| ID | Capability | UI | Data | Status | Issue | Contract |
|----|-----------|----|------|--------|-------|----------|
| GOV-1 | The $40 trillion — debt series, ten-year doubling, nominal / % of GDP toggle. The standfirst names the two units the toggle switches between and leaves the $40 trillion to the finding. **#59** moved `nominal`'s marker into this standfirst from §10, 8,062 characters later — the route's worst first-use gap — and marked `GDP` beside it on the toggle sentence, and `CBO` in the closing paragraph. | done | done | Shipped | #1, #35, #36, #53, #59 | contracts/interfaces/budget-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-2 | Who holds it — public vs intragovernmental, domestic vs foreign. Country holdings are fetched from Treasury International Capital's Major Foreign Holders table at a release month pinned in curated YAML, not curated constants and not an outlet's paraphrase; the split and the totals stay curated from Debt to the Penny, so the output is `refresh.mode: "mixed"` and renders both dates. **#59** moved `intragovernmental`'s marker up into the finding, where the reader meets the word first, and reworded the prose below it: "the intragovernmental piece is real money owed to future retirees" became "a genuine obligation", because that "real" is the everyday adjective and marking it would have pointed the reader at the economic term. The `real` marker stays where the route's first technical use of it is. | done | done | Shipped | #6, #54, #59 | contracts/interfaces/curated-snapshots.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-3 | How old is the debt — maturity ladder against the 30-year ceiling. The figure's accessible name is the section's finding rather than a description of the drawing. Instrument composition and the marketable total are fetched from the Monthly Statement of the Public Debt at a statement month pinned in curated YAML, not curated constants credited to a compiler of that statement; average maturity stays curated from the JEC update, so the output is `refresh.mode: "mixed"` and renders both months. Since #58 the figure note meets Criterion 3's 45-word cap: the 49-word instrument-family sentence is three, and what the chart does not draw is said in its own clause | done | done | Shipped | #6, #53, #56, #58 | contracts/interfaces/curated-snapshots.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-4 | The deficit is the part of the budget revenue did not cover — outlays stacked (mandatory net/discretionary/interest), revenue line, deficit below zero, party-control strip, unit toggle, year inspection, era bands. The heading states what the single axis shows rather than that there is one. **#59** moved `net interest`'s marker up into the finding from the paragraph below it. | done | done | Shipped | #2, #52, #59 | contracts/interfaces/budget-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-5 | The structural gap — revenue vs outlays, nominal / real / % of GDP toggle, sign-and-position surplus band | done | done | Shipped | #5 | contracts/interfaces/budget-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-6 | What Congress votes on — mandatory (net) vs discretionary vs net interest, nominal / real / % of GDP toggle, FY2015 trajectory marker. Since #58 the closing prose takes a comma pair rather than an em dash pair, and no longer serves "mandatory spending—" or "permanent law andnet interest" — both were the same Astro whitespace collapse. Criterion 3 | done | done | Shipped | #5, #34, #58 | contracts/interfaces/budget-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-7 | Net interest — nominal/real bars by fiscal year, FY2015 trough and FY2003 series low marked, cross-linked to debt maturity | done | done | Shipped | #5 | contracts/interfaces/budget-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-8 | The laws — sortable/filterable explorer, cost, vote, enactment date. Each law's `verified_source` names the primary record alone; the TCJA cites the House Clerk roll call and nothing beside it | done | done | Shipped | #3, #31, #32, #33, #56 | contracts/interfaces/law-data.md, contracts/interfaces/charts.md, contracts/interfaces/budget-data.md, contracts/prose.md |
| GOV-9 | Who passed it, who signed it — the same $16.75T by voting coalition and by signing president, Radix Tabs. The finding is the one thing the two columns together show, that they total the same number; the eight per-bar figures are in the closing prose | done | done | Shipped | #4, #33, #53 | contracts/interfaces/attribution.md, contracts/interfaces/budget-data.md, contracts/prose.md |
| GOV-10 | Where the money comes from — revenue by source (nominal/%GDP/%share toggle), OECD comparison. **#59** marks `OECD` in the paragraph that introduces the cross-country figure, which is also where the section says the two revenue measures are not two readings of one number. | done | done | Shipped | #7, #34, #59 | contracts/interfaces/revenue-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-11 | By state, and which states give more than they get — IRS gross collections vs USASpending award spending, tile-grid cartogram, sortable table, state tax mix. The Rockefeller Institute balance-of-payments study is cited and never ingested, ruled and dated in the contract rather than left as a practice. Since #58 the section meets Criterion 3 throughout: the standfirst's dash is a colon, the give/get note's is a full stop, the place-of-payment paragraph's is a full stop with the limit link opening the sentence after it, and the three-vintages paragraph's two sentences over the 45-word cap are five. The cartogram legend's own `.prose`, which `StateGiveGet.tsx` renders into the page, took the same colon. **#59** marks `IRS` at the dating-exception paragraph, this route's first occurrence. | done | done | Shipped | #14, #56, #58, #59 | contracts/interfaces/state-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| GOV-12 | What this cannot tell you — six limits in full, no disclosure wrapper, plus the concentration and deficit-vs-debt items. Limit 6 owns the place-of-payment principle (filer address vs place of performance) that §11 used to restate locally; §11 now links to it and keeps only its worked examples | done | n/a (prose) | Shipped | #8, #39 | contracts/interfaces/content-sources.md, contracts/prose.md |

## Households route (`src/pages/households/index.astro`)

| ID | Capability | UI | Data | Status | Issue | Contract |
|----|-----------|----|------|--------|-------|----------|
| HH-1 | What a household earns — real median household income, 1984-2024, constant 2024 dollars, brushable year range. The standfirst says what is plotted, in what units and over what window; the 28.1% it used to quote is the finding's. Since #58 both the figure note and the closing prose meet Criterion 3: an appositive comma pair for the deflator gloss, a colon for the amplifying clause about the unobserved years | done | done | Shipped | #9, #53, #58 | contracts/interfaces/income-inequality-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| HH-2 | The spread — family Gini index 1947-2024 and CBO top 1% income share (two published points), shared brushable year range. The standfirst names the two series, their two publishers and the fact that one is annual and one is two anchor points, rather than pre-claiming the doubling the finding reports. Since #58 the figure note's em dash is an appositive comma pair. Criterion 3. **#59** marks `CBO` in the standfirst, this route's first occurrence. | done | done | Shipped | #9, #53, #58, #59 | contracts/interfaces/income-inequality-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| HH-3 | A century of brackets — bracket count, thresholds in constant 2024 dollars, top statutory rate vs the schedule ladder top, 1913-2025. The finding is one claim, the real 96% fall in the top-bracket threshold; the two dollar thresholds it used to carry are in the closing prose. Since #55 the top-rate series is anchored on IRS SOI Historical Table 23 rather than merely citing it: `curated/top_rates_soi_anchor.yaml` holds Table 23's highest-bracket column for 1913-2018 with its SHA-256, and `check_top_rates_anchor` fails the build on any disagreement (all 106 overlapping years agree). The 1913-2019 LADDER still comes from the Tax Foundation compilation, kept deliberately and now documented as a derivation of Table 23 and the Revenue Procedures, because no IRS release publishes the ladder at per-year, per-filing-status, per-bracket granularity; Tax Policy Center is gone from the citation. Since #58 the section meets Criterion 3: the note's ` -- ` pair is parentheses around what is itself a list of years, "the bracket COUNT" is `<strong>count</strong>`, "Surtaxes ARE folded" is recast to "Surtaxes are the exception", and the threshold sentence no longer serves "set innominal dollars". **#59** moved both `statutory rate` and `nominal` up into this standfirst, from §4 and from the paragraph below the chart: the standfirst names both a full section and 489 characters before their markers sat. | done | done | Shipped | #10, #53, #55, #58, #59 | contracts/interfaces/bracket-history-data.md, contracts/prose.md |
| HH-4 | Statutory is not effective — CBO average federal tax rate anchor points (includes payroll tax) against the top statutory rate. The 253-character finding is one claim in 156 characters, and the closing prose now says outright that CBO's measure counts more than the income tax. The statutory line this section draws is `curated/top_rates.yaml`, the same two-route file that feeds `income_inequality.top`, so #55's Table 23 anchor and its `check_top_rates_anchor` guard cover this row's series too. Since #58 the standfirst's ` -- ` pair is an appositive comma pair and no longer serves "far less than thestatutory rate", and the note is recast so that CBO's broader base is carried by word order instead of shouted capitals — a `note=` prop is a plain attribute rendered as text and cannot carry `<strong>`. Criterion 3 | done | done | Shipped | #10, #53, #55, #58 | contracts/interfaces/bracket-history-data.md, contracts/prose.md |
| HH-5 | Who pays the income tax — AGI share paired with tax share by percentile group, and the five-year top-1% tax-share history. The standfirst says how filers are ranked and what each of the two charts does; the finding claims the top 1% alone, against the first chart, and the other groups moved into the closing prose. Since #58 the who-pays note's em dash pair is parentheses around the nesting gloss, and the closing prose no longer serves "overstates theeffective rate". Criterion 3. **#59** marks `IRS` and `FY1995` in the closing paragraph, where the route says out loud that its tax-year dating breaks the site's fiscal-year convention. | done | done | Shipped | #11, #53, #58, #59 | contracts/interfaces/income-tax-by-group-data.md, contracts/interfaces/charts.md, contracts/prose.md |
| HH-6 | The bill you do not see — payroll tax as a share of GDP and of total revenue, FY1962-FY2025, closing on a paragraph that says what follows from the finding: outside the top decile the payroll bill is the larger of the two, and Section 5's chart counts none of it. Since #58 the figure note's em dash is a full stop. Criterion 3. **#59** marks `GDP` in the finding, this route's only occurrence of it. | done | done | Shipped | #11, #52, #58, #59 | contracts/interfaces/charts.md, contracts/prose.md |
| HH-7 | Limits — income-tax-only scope, families-not-households Gini basis, statutory-vs-effective, tax-unit basis. Since #58 limit 4's em dash is a comma. Criterion 3 | done | n/a | Shipped | #11, #58 | contracts/prose.md |

## Reference pages

| ID | Capability | UI | Data | Status | Issue | Contract |
|----|-----------|----|------|--------|-------|----------|
| REF-1 | /sources renders SOURCES.md in full, including known discrepancies — and the register is now **gated, not merely rendered**: every source cited by a published output's `_meta.source` must be findable in SOURCES.md or the build fails (see DATA-3). **Since #57 every source also states what KIND of source it is and where a reader goes next**: all 24 register entries carry a `tier` from a five-term vocabulary (`primary`, `official republication`, `scholarly republication`, `compilation`, `secondary`) and an `https://` `url` or a written `url_exempt` reason, gated by `check_sources` rules F–I — rule I matching the composed lead-in **into** SOURCES.md, so the document is still never parsed out of. The page's `## Primary` heading, which was false over Tax Foundation and FRED, is retired in favour of the per-source tier. `/sources` carried **zero** hyperlinks in `main` before #57 and carries 23 now, and no longer scrolls sideways at 390px (#79, re-measured 390 = 390) | done | done | Shipped | #8, #39, #57 | contracts/interfaces/content-sources.md, contracts/prose.md, contracts/accessibility.md |
| REF-2 | /glossary defines every technical term the three routes use, one per file, each at a durable anchor derived from its filename — alphabetical, letter-grouped, server-rendered with zero islands, and **gated by a Zod schema plus five build-time throws**: an empty collection, a Markdown body nothing renders, an id collision, a dangling `see_also`, and a `first_used` naming a section its route does not have all fail the build rather than shipping a page that looks right. `src/data/sections.ts` is the one map of route → section anchors the last of those checks against. **Its first in-prose consumer is `src/components/Term.astro` (A11Y-6, #47)**: the 180-character `short` cap now sizes a popover in three routes' prose, the filename-derived anchor is what every marker links to, and a `<Term id>` naming no entry is a sixth build-time throw. **Since #50 a term's `source` is a reference, not prose**: a non-empty list of `pipeline/curated/sources.yaml` `registry:` keys, resolved to the register's `registered_as` verbatim by `src/data/source-register.ts` and never stored under `src/`, so a vintage bump in `SOURCES.md` moves the glossary with no glossary edit. Gated in two layers — `z.enum` over the register's keys, so an unresolvable key is a schema failure and the raw key has no path to the page, and `check_glossary_sources` in `pipeline/lib/validate.py`, called unconditionally from `run()` so the unattended pipeline lane is not blind to it. `check_sources` rule C now spans outputs **and** glossary terms, which makes deleting the sole term citing a source a build failure rather than a silent orphan. **#50's "zero external hyperlinks" boundary is lifted by #57**, which is the issue #50 named as the one that would lift it: the `.transform` now resolves `links` beside `text`, so each cited source renders as a link to its registered URL with its tier beside it. The verbatim line is unchanged and still printed first — the links are an addition to it, never a replacement, so "nothing but the register's own string is ever printed" still holds of the line. **#59 took the collection to 29 entries and added the `abbr` field**: the short forms a reader meets in place of the display `term` — `CBO`, `CPI-U`, `intragovernmental` — declared as data rather than kept in a map beside the prose. It is load-bearing twice. `pipeline/tests/test_prose.py`'s Criterion 4 checker searches the served prose for `term` plus every `abbr`, so declaring one can turn a green page red, which is the point; and every all-caps surface form is derived out of this collection into `REGISTERED_INITIALISMS`, whose hand-named remainder the same file asserts is **disjoint** from it, so an acronym that gains an entry leaves the named list in the same commit. Six of the 29 are the acronym entries #59 added (CBO, GDP, CPI, PCE, IRS, OECD), each with the expansion as `term` and the acronym as `abbr`; `FY` is an `abbr` on the existing `fiscal-year` rather than a seventh file, because two entries for one concept is the failure `abbr` exists to prevent | done | done | Shipped | #45, #50, #57, #59 | contracts/interfaces/glossary.md, contracts/interfaces/content-sources.md, contracts/prose.md |
| REF-3 | /contents indexes the whole site on one page — every route, every section, every numbered figure with its number, title and source, and every glossary term with a link to its definition and to its first use. **Nothing on it is hand-listed**: routes and labels come from `siteRoutes`, sections from the arrays each route already renders its own contents list from, figures from `src/data/figures.ts`, terms from the glossary collection. Adding a section, a route, a figure or a term changes this page in the same build with no edit to it. The figure manifest is what made the figure half possible: the number was a CSS counter (`counter(figure)`) that existed only in the rendered layout, so no page could read it — it is now a build-time value the call sites and the index render from the same entry. `join` and the route list also move out of `BaseLayout` here, so the site has one base-path join rather than three. Since #58 the standfirst's em dash is a full stop, and its interpolated figure count no longer fuses with the word before it: `{' '}` now carries the space the line break was dropping, which is the one form of that defect no check on this repository can see. Criterion 3 | done | done | Shipped | #49, #58 | contracts/interfaces/contents.md, contracts/interfaces/charts.md, contracts/prose.md |

## Cross-cutting

| ID | Capability | UI | Data | Status | Issue | Contract |
|----|-----------|----|------|--------|-------|----------|
| DATA-1 | Every pipeline output is JSON-Schema validated on every build; an output with no schema fails the build rather than being skipped, so coverage cannot silently lapse as the population grows | n/a | done | Shipped | #14, #37 | contracts/interfaces/*.md § Schema |
| DATA-2 | The pipeline emits every CBO price series the sections need (CPI-U, chained CPI-U, core CPI-U, core PCE), rejects the one known corrupt upstream bracket row loudly at ingest and would catch a new duplicate bracket floor on the published output, and drift-checks both halves of the debt split | n/a | done | Shipped | #38 | contracts/interfaces/economy-data.md, contracts/interfaces/bracket-history-data.md |
| DATA-3 | Every source a published output cites is registered in SOURCES.md, the document /sources renders in full; the reconciliation runs unconditionally on every build, so a cited-but-unregistered source fails the build rather than shipping unnoticed. **Nine rules since #57**, all inside `check_sources` and none behind an `if`: A–D reconcile the source line, E bans an outlet from sourcing an emitted value (#54), and F–I gate the tier, the URL, the `compiled_from`/`justification` a tier demands, and the agreement between the register and SOURCES.md. Each of F–I is proved to bite by an executed negative test | n/a | done | Shipped | #39, #54, #57 | contracts/interfaces/content-sources.md § Every cited source is registered, § A source states what kind of source it is |
| DATA-4 | Every pipeline HTTP request goes through one cached, fail-loud core — text, binary and POST-JSON callers are thin wrappers over the same cache, timeout, status and truncated-body discipline, so it cannot drift between them; there is no retry, by design | n/a | done | Shipped | #40 | contracts/interfaces/pipeline-http.md |
| DATA-5 | Every `_meta.title` year range is derived from `_meta.coverage` rather than typed by hand; a literal year in a curated title, a published range that names no declared coverage window, and a range with no coverage behind it all fail the build. One exemption, `cbo_effective_rates`, carries its written reason in the check | n/a | done | Shipped | #41 | contracts/interfaces/budget-data.md, contracts/interfaces/income-inequality-data.md, contracts/interfaces/curated-snapshots.md |
| DATA-6 | `BRIEF.md`'s "Files in this folder" manifest matches the files on disk, and cannot drift silently again — every path it lists is asserted to resolve, and the brief is named as the brief rather than as `README.md` | n/a | n/a | Shipped | #41 | — (BRIEF.md is itself the contract) |
| A11Y-1 | Shared-layer accessibility: no-JS data tables, named figure/nav landmarks, focusable skip target, SVG focus ring, JS-off chart legibility, token contrast enforcement, static conformance suite | done | n/a | Shipped | #15 | contracts/accessibility.md |
| A11Y-2 | Keyboard and assistive-technology sweep across all three routes and `/sources` — executed in a browser 2026-08-24 and 2026-08-26 (Chrome 151, WebKit 26.5): keyboard traversal, roving tabindex and focus restoration, 390px with scripting on and off, rendered-pixel contrast, and the greyscale pass with computed per-chart luminance ratios; results in `contracts/accessibility.md` § Manual pass results, FAILs filed as #62–#66, #69–#79. Remaining: the screen-reader passes on all four pages and the focus-ring check in Safari.app, which no agent can execute — **#80 blocks `Shipped`** | none | n/a | In progress | #30 | contracts/accessibility.md |
| A11Y-3 | Narrow-viewport navigation — below 62rem a bar fixed to the top of the viewport carries the site title, the current route name and a native `<details>` disclosure holding all five route links and the page's full contents list, internally scrolled; one tab stop, placed after the skip link and before `main`; focus moves in on open and Escape returns it to the trigger, with no focus trap by design; fully operable with scripting off; the desktop rail and its DOM are untouched. The `<summary>` is also the site's **back-to-contents affordance at every scroll depth** below 62rem — `position: fixed`, so it is on screen from the deepest section of every route, measured 83.8/107.0/109.5 × 44 CSS px from `#limits` on `/economy`, `/households` and `/government`; #46's folding call is recorded against it and builds no second control | done | n/a | Shipped | #42 | contracts/accessibility.md |
| A11Y-4 | Reading position in the contents list — one `IntersectionObserver` marks the section containing the viewport midpoint with `aria-current="true"` on both the desktop rail list and the narrow-viewport panel list, from one `querySelectorAll` so the two cannot drift; the lowest section in document order whose top edge is at or above the midpoint, with the document top and bottom decided by scroll position so section 1 and `limits` are always reachable; monotonic and non-oscillating; no scripted scrolling anywhere, so `prefers-reduced-motion` is vacuous; nothing in the built HTML and nothing marked with scripting off; no live region, so rapid scrolling announces nothing | done | n/a | Shipped | #44 | contracts/accessibility.md |
| A11Y-5 | Reading position across navigation — Back and Forward restore the reader's position on all routes at `history.scrollRestoration`'s `'auto'` default, with **no storage, no assignment and no scripted scroll of our own**; returning to a URL with a hash restores the position rather than re-jumping to the fragment; scroll anchoring absorbs both the post-hydration chart growth (~115–240px) and the 11,854px shrink from collapsed `<details>` tables, and the #44 spy marks the restored section rather than the pre-restore one. Held by five static guards against the declarations that would silently remove it. Above 62rem the sticky rail is the contents affordance (636px fully in view from `#limits` at 1440×900); below it, #42's bar trigger is. **Open on WebKit**, which has no scroll anchoring: plain Back drifts up to 238.7px (28% of an 844px viewport, ships as-is) but the collapsed-tables case lands ~6,800px short — measured and recorded in the contract, not repaired | done | n/a | Shipped | #46 | contracts/accessibility.md |
| A11Y-6 | In-prose glossary term markers — each term's first use per page is wrapped in `<Term>`, a real `<a href>` to its `/glossary` anchor with the term's one-sentence `short` as a **DOM-descendant** popover opened on hover, on keyboard focus and on a tap. 26 markers: 6 on `/economy`, 8 on `/households`, 12 on `/government`. **Native, no Radix primitive and no new dependency** — the third time this repository has chosen native over a Radix disclosure, for the reason #15 paid for. **Zero islands and zero `client:` directives**: one `is:inline` IIFE in `BaseLayout.astro` shared by every marked term on the page. WCAG 1.4.13 *hoverable* is met by the DOM shape rather than a timer, *persistent* by there being no auto-close timer at all, and *dismissable* by an Escape bound to the wrapper rather than to `document`, so it does not contend with the nav panel or the three `/government/` filter dropdowns. Not clipped at any viewport by construction — the popover is `left: 0; right: 0` against its paragraph, so it is the width of the prose. With scripting off every marker is a plain followable link, never a `<button>` and never inert, and the definition is still the trigger's `aria-describedby` target. Whether that description *announces* is the screen-reader half and is **NOT EXECUTED** — carried by #80 through A11Y-2, as A11Y-3's disclosure is. **#59 proved the markers sit where a reader first meets each term**, which #47 left as a reading: `test_every_marked_term_sits_at_its_first_use` asserts, over the served bytes and with no baseline, that no marked term's surface form — `term` plus the new `abbr` values — appears in `prose`, `standfirst` or `finding` earlier than its marker on that route, and `test_every_content_route_marks_every_glossary_term_it_uses` asserts the per-route half. Seven markers were wrong and all seven moved; one of the seven was a **reword** rather than a move, because `/government` §2's "real money" is the everyday adjective and marking it would have pointed the reader at the economic term. **38 markers now: 10 on `/economy`, 12 on `/households`, 16 on `/government`**, up from 26, and four of them sit inside a `.finding`, which #47 had excluded — the finding-equals-`aria-label` guarantee survives because `_deep_text` skips `.term-pop`, so wrapping a word changes no served text | done | n/a | Shipped | #47, #59 | contracts/accessibility.md, contracts/interfaces/glossary.md, contracts/prose.md |
